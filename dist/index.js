var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// src/data/alias.json
var alias_default = {
  _comment: "Maps alternate/variant names found in README files to the canonical names used in the reference JSON files. Keys are the aliases (lowercased for matching); values are the canonical display names.",
  extruders: {
    CW1: "Clockwork1",
    "Clockwork 1": "Clockwork1",
    "Afterburner Clockwork": "Clockwork1",
    CW2: "Clockwork2",
    "Clockwork 2": "Clockwork2",
    "lgx-lite": "LGX-Lite",
    "lgx lite": "LGX-Lite",
    lgx_lite: "LGX-Lite",
    "bondtech lgx lite": "LGX-Lite",
    "bondtech lgx-lite": "LGX-Lite",
    "Large Gear Clockwork 2": "Large Gear Clockwork2",
    "Large Gear CW2": "Large Gear Clockwork2",
    Galileo2: "Galileo 2",
    G2SA: "Galileo 2 Standalone",
    "G2 extruder": "Galileo 2",
    "Galileo 2 SA": "Galileo 2 Standalone",
    "Galileo2 SA": "Galileo 2 Standalone",
    "Galileo2 Standalone": "Galileo 2 Standalone",
    "G2 Standalone": "Galileo 2 Standalone",
    "Galileo 2 stand-alone": "Galileo 2 Standalone",
    "Orbiter V2": "Orbiter 2",
    "Orbiter v2": "Orbiter 2",
    "Orbiter 2.0": "Orbiter 2",
    "Orbiter V2.0": "Orbiter 2",
    "Orbiter v2.0": "Orbiter 2",
    "Orbiter V2.5": "Orbiter 2.5",
    "Orbiter v2.5": "Orbiter 2.5",
    "Orbiter V1.5": "Orbiter 1.5",
    "Orbiter v1.5": "Orbiter 1.5",
    "Sherpa-Mini": "Sherpa Mini",
    Sherpa_Mini: "Sherpa Mini",
    "Sherpa-Micro": "Sherpa Micro",
    Sherpa_Micro: "Sherpa Micro",
    "WW-BMG": "Wristwatch BMG",
    WWBMG: "Wristwatch BMG",
    "Wristwatch-BMG": "Wristwatch BMG",
    "WW-G2": "Wristwatch G2",
    WWG2: "Wristwatch G2",
    "Wristwatch-G2": "Wristwatch G2",
    DFA: "Double Folded Ascender",
    "Folded Ascender": "Double Folded Ascender",
    "VZ HextrudORT": "Vz HextrudORT",
    "VZ-HextrudORT": "Vz HextrudORT",
    "Vz-HextrudORT": "Vz HextrudORT",
    VZHextrudORT: "Vz HextrudORT",
    Hextrudort: "HextrudORT",
    RoundHouse: "RoundHouseBMG",
    "Round House": "RoundHouseBMG",
    "Papilio-Lite": "Papilio Lite",
    "Bondtech BMG": "BMG",
    "BMG extruder": "BMG",
    "Micro-Cosm": "Micro-cosm",
    Microcosm: "Micro-cosm",
    "TBG Lite": "RoundTrip",
    "LGX-Lite V2": "LGX-Lite",
    "Large Gear Clockwork2": "Large Gear Clockwork2"
  },
  hotends: {
    "E3D Revo": "Revo",
    "Revo Voron": "Revo",
    "E3D Revo Voron": "Revo",
    "Revo 6": "Revo",
    "E3D V6": "E3D V6",
    "Phaetus Dragon HF": "Dragon HF",
    "Phaetus Dragon UHF": "Dragon UHF",
    "Phaetus Dragon SF": "Dragon SF",
    "Phaetus Dragon Ace": "Dragon Ace",
    "Dragon Hotend HF": "Dragon HF",
    "Dragon Hotend UHF": "Dragon UHF",
    "Dragon Hotend SF": "Dragon SF",
    "NF Crazy": "NF-Crazy",
    "Mellow NF-Crazy": "NF-Crazy",
    "Mellow NF Crazy": "NF-Crazy",
    "NF-Crazy V2": "NF-Crazy",
    "NF Crazy V2": "NF-Crazy",
    "NF Crazy Volcano": "NF-Crazy",
    "Bambu X1C": "Bambu X1/P1",
    "Bambu P1P": "Bambu X1/P1",
    "Bambu P1S": "Bambu X1/P1",
    "Bambu Hotend": "Bambu X1/P1",
    "X1C Hotend": "Bambu X1/P1",
    "Bambu X1": "Bambu X1/P1",
    "Rapido Plus": "Rapido UHF",
    "Rapido+": "Rapido UHF",
    "Rapido 2 UHF": "Rapido UHF",
    "Rapido 2 HF": "Rapido HF",
    "Dragonfly BMS": "Dragonfly",
    "Dragonfly BMS2": "Dragonfly",
    "TZ 2.0": "TZ2.0",
    "TZ-2.0": "TZ2.0",
    "TriangleLab TZ2": "TZ2.0",
    "TZ-3.0": "TZ 3.0/4.0",
    "TZ-4.0": "TZ 3.0/4.0",
    "TZ 3.0": "TZ 3.0/4.0",
    "TZ 4.0": "TZ 3.0/4.0",
    "Mellow NF Zone": "NF-Crazy",
    "Red Lizard K1-HF": "Red Lizard K1 HF",
    "Red Lizard K1-UHF": "Red Lizard K1 UHF",
    "Creality Spider Pro": "Dragon SF",
    "VzBot Goliath": "Goliath",
    VzGoliath: "Goliath",
    "Chube Hotend": "Chube",
    "Dragon ACE": "Dragon Ace",
    "Dragon Ace Volcano": "Dragon Ace Volcano",
    "Bambu X1/P1 hotend": "Bambu X1/P1"
  },
  probes: {
    "Voron TAP": "TAP",
    "Voron Tap": "TAP",
    Tap: "TAP",
    "Tap Probe": "TAP",
    "Voron Tap Probe": "TAP",
    "Klicky-PCB": "Klicky PCB",
    "PCB Klicky": "Klicky PCB",
    "Klicky-pcb": "Klicky PCB",
    "Whopping Klicky": "Klicky PCB",
    "BL Touch": "BLTouch",
    BL_Touch: "BLTouch",
    "Beacon Rev D": "Beacon",
    "Beacon RevD": "Beacon",
    "Beacon Rev H": "Beacon",
    "Beacon Rev H2": "Beacon",
    Carto: "Cartographer",
    Cartographer3D: "Cartographer",
    IDM: "IDM Scanner",
    "IDM Probe": "IDM Scanner",
    "Mellow Eddy": "Mellow Scanner",
    "FLY Eddy": "Mellow Scanner",
    "Fly Eddy": "Mellow Scanner",
    "BTT Eddy": "Eddy",
    "Bigtreetech Eddy": "Eddy",
    "Omron Probe": "Omron",
    "ZeroClick Probe": "ZeroClick",
    "Zero Click": "ZeroClick",
    "Super PINDA": "SuperPINDA",
    "Super Pinda": "SuperPINDA",
    "SideSwipe Probe": "SideSwipe",
    "Slide Swipe": "SlideSwipe",
    "Euclid Probe": "Euclid",
    "BD-Sensor": "BD Sensor",
    BDSensor: "BD Sensor"
  },
  boards: {
    EBB36: "EBB36",
    "BTT EBB36": "EBB36",
    "BTT EBB 36": "EBB36",
    "EBB 36": "EBB36",
    "EBB36 CAN": "EBB36",
    "BTT EBB36v1": "EBB36",
    "BTT EBB36 v1.2": "EBB36",
    EBB42: "EBB42",
    "BTT EBB42": "EBB42",
    "BTT EBB 42": "EBB42",
    "EBB 42": "EBB42",
    "EBB42 CAN": "EBB42",
    "EBB SB2209": "EBB SB2209",
    "BTT EBB SB2209": "EBB SB2209",
    SB2209: "EBB SB2209",
    "EBB SB2240": "EBB SB2240",
    "BTT EBB SB2240": "EBB SB2240",
    SB2240: "EBB SB2240",
    HUVUD: "Huvud",
    Huvud: "Huvud",
    "BTT Huvud": "Huvud",
    "Fly SHT36": "Fly SHT36",
    "FLY-SHT36": "Fly SHT36",
    "Mellow Fly SHT36": "Fly SHT36",
    "Mellow SHT36": "Fly SHT36",
    "FLY SHT36": "Fly SHT36",
    "Fly SHT42": "Fly SHT42",
    "FLY-SHT42": "Fly SHT42",
    "Mellow Fly SHT42": "Fly SHT42",
    "Mellow SHT42": "Fly SHT42",
    "FLY SHT42": "Fly SHT42",
    "Nitehawk USB": "Nitehawk USB",
    "Nitehawk-USB": "Nitehawk USB",
    "LDO Nitehawk USB": "Nitehawk USB",
    "LDO Nitehawk-USB": "Nitehawk USB",
    "Nitehawk-SB": "Nitehawk-SB",
    "Nitehawk SB": "Nitehawk-SB",
    "LDO Nitehawk-SB": "Nitehawk-SB",
    "LDO Nitehawk SB": "Nitehawk-SB",
    Nitehawk36: "Nitehawk USB",
    SB2040: "SB2040",
    "BTT SB2040": "SB2040",
    "BTT SB 2040": "SB2040",
    Picobilical: "Picobilical",
    "Voron Picobilical": "Picobilical",
    "Hartk Picobilical": "Picobilical",
    "toolhead PCB": "Toolhead PCB",
    "Toolhead PCB": "Toolhead PCB",
    "CANBUS board": "Toolhead PCB",
    "CAN toolhead": "Toolhead PCB",
    "EBB CAN": "EBB36"
  },
  printers: {
    "Voron 0": "V0",
    "Voron Zero": "V0",
    "V0.1": "V0",
    "V0.2": "V0",
    "V0.2r1": "V0",
    Voron0: "V0",
    "Voron 2.4": "V2.4",
    "Voron2.4": "V2.4",
    "Voron 2": "V2.4",
    "2.4r2": "V2.4",
    "V2.4r1": "V2.4",
    "V2.4r2": "V2.4",
    VT: "Trident",
    "Voron Trident": "Trident",
    "Voron Switchwire": "Switchwire",
    "Voron SW": "Switchwire",
    "Sovol SV08": "SV08",
    "Voron Enderwire": "Enderwire",
    "Ender Wire": "Enderwire"
  }
};

// src/data/extra_github_locations.seed.json
var extra_github_locations_seed_default = {
  extra_locations: [
    {
      toolhead: "Hypernova",
      url: "https://github.com/Hyperdrive-Design/hyperdrivedesign.com/blob/main/docs/products/Hypernova/hypernova.md"
    },
    {
      toolhead: "Hypernova BSE",
      url: "https://github.com/Hyperdrive-Design/hyperdrivedesign.com/blob/main/docs/products/Hypernova/hypernova_bedsligers.md"
    }
  ]
};

// src/data/extruders.json
var extruders_default = {
  extruders: [
    {
      name: "Clockwork1",
      mounting_pattern: "Afterburner",
      gear_type: "BMG",
      url: "https://github.com/VoronDesign/Voron-Afterburner/releases",
      description: "Direct drive extruder for the Voron Afterburner toolhead.",
      filament_sensor: "unknown"
    },
    {
      name: "Clockwork2",
      mounting_pattern: "Stealthburner",
      gear_type: "BMG",
      url: "https://github.com/VoronDesign/Voron-Stealthburner",
      description: "Direct drive extruder for the Voron Stealthburner toolhead, IDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Pocket Watch",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://github.com/VoronDesign/Voron-0/tree/Voron0.0/VORON_Pocketwatch",
      description: "Bowden extruder for Voron V0.",
      filament_sensor: "unknown"
    },
    {
      name: "Pocket Watch Nema17",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://mods.vorondesign.com/details/1rw8IyTJO2hoMYllya1IgQ",
      description: "Bowden extruder modeled from Clockwork1.",
      filament_sensor: "unknown"
    },
    {
      name: "Night Watch",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://github.com/VoronDesign/Pocket-Watch/tree/main",
      description: "Bowden extruder designed from Clockwork2 for Voron V0.2.",
      filament_sensor: "unknown"
    },
    {
      name: "Dual Nightwatch",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://github.com/hartk1213/MISC/tree/main/Voron%20Mods/Extruders/Dual_Nightwatch",
      description: "Bowden extruder, variant of Night Watch, features dual extruder design.",
      filament_sensor: "unknown"
    },
    {
      name: "M4 Mobius",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://github.com/VoronDesign/Mobius-Extruder",
      description: "Bowden belt driven extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Mini14",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://github.com/hartk1213/MISC/tree/main/Voron%20Mods/Extruders/Mini14",
      description: "Bowden belt driven extruder, variant of M4, features smaller size and nema14 motor.",
      filament_sensor: "unknown"
    },
    {
      name: "Wristwatch BMG",
      mounting_pattern: "Orbiter 2",
      gear_type: "BMG",
      url: "https://github.com/bythorsthunder/Voron_Mods/tree/main/Wristwatch_Extruder_BMG",
      description: "Direct drive standalone extruder, variant of Wristwatch G2, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "WWBMG IDGA",
      mounting_pattern: "Orbiter 2",
      gear_type: "BMG",
      url: "https://github.com/minilogique/ww-extruder-for-idga",
      description: "Direct drive standalone extruder, variant of Wristwatch BMG, IDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Mini",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/Annex-Engineering/Sherpa_Mini-Extruder",
      description: "Direct drive standalone extruder, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Micro",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/Annex-Engineering/Sherpa_Micro-Extruder",
      description: "Direct drive standalone extruder, smaller and lighter Sherpa Mini, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Heavy",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/Annex-Engineering/Sherpa_Heavy-Extruder",
      description: "Direct drive standalone extruder, variant of Sherpa Mini, uses a nema17 with pinion gear, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Extra Heavy",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://www.printables.com/model/549890-sherpa-extra-heavy-with-nema17-update-2",
      description: "Direct drive standalone extruder, variant of Sherpa Heavy using a standard nema17, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Crew Mini",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/jrlomas/Sherpa-Crew-Mini",
      description: "Direct drive standalone extruder, variant of Sherpa Mini, features two sets of BMG gears.",
      filament_sensor: "unknown"
    },
    {
      name: "Sherpa Zero",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/jrlomas/Sherpa-Zero",
      description: "Direct drive standalone extruder, variant of Sherpa Mini, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Shepherd",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/odbee/shepherd/tree/main",
      description: "Direct drive extruder for Archetype Breakneck part cooling duct, variant of Sherpa Mini.",
      filament_sensor: "unknown"
    },
    {
      name: "Fz",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/FZaii/FZ-Extruder",
      description: "Direct drive standalone extruder, variant of Sherpa Mini.",
      filament_sensor: "unknown"
    },
    {
      name: "Miro",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/jrlomas/miro-extruder",
      description: "Direct drive standalone extruder, features dual-drive gears.",
      filament_sensor: "unknown"
    },
    {
      name: "Sailfin",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/CroXY3D/Sailfin-Extruder",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Sharkfin",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/KayosMaker/Sharkfin_Extruder",
      description: "Direct drive standalone extruder, variant of Sailfin, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "Sharketype",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/Armchair-Heavy-Industries/Sharketype",
      description: "Direct drive standalone extruder for Archetype toolhead, variant of Sharkfin, RIDGA supported.",
      filament_sensor: "unknown"
    },
    {
      name: "HextrudORT",
      mounting_pattern: "other",
      gear_type: "BMG",
      url: "https://github.com/MirageC79/HextrudORT",
      description: "Direct drive standalone extruder for the HevORT toolhead.",
      filament_sensor: "unknown"
    },
    {
      name: "Vorxtrudort",
      mounting_pattern: "other",
      gear_type: "BMG",
      url: "https://github.com/nhchiu/VoronMods/tree/main/Extruders/Vorxtrudort",
      description: "Direct drive standalone extruder, variant of HextrudORT with mounting for Voron.",
      filament_sensor: "unknown"
    },
    {
      name: "Vz HextrudORT",
      mounting_pattern: "other",
      gear_type: "BMG",
      url: "https://github.com/VzBoT3D/Vz-HextrudORT",
      description: "Direct drive standalone extruder, variant of HextrudORT for VzBot.",
      filament_sensor: "unknown"
    },
    {
      name: "BoomBox",
      mounting_pattern: "Bowden",
      gear_type: "BMG",
      url: "https://www.printables.com/model/1236812-boombox-the-best-bowden-extruder-on-earth",
      description: "Bowden extruder, features dual-gears and dual-motors.",
      filament_sensor: "unknown"
    },
    {
      name: "RoundHouseBMG",
      mounting_pattern: "Sherpa Mini",
      gear_type: "BMG",
      url: "https://github.com/chirpy2605/voron/tree/main/general/RoundHouse",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Hummingbird",
      mounting_pattern: "LGX-lite",
      gear_type: "HGX-lite 1.0",
      url: "https://github.com/nhchiu/VoronMods/tree/main/Extruders/Hummingbird",
      description: "Direct drive standalone extruder, features LGX-lite mounting.",
      filament_sensor: "unknown"
    },
    {
      name: "Large Gear Clockwork2",
      mounting_pattern: "Stealthburner",
      gear_type: "HGX-lite 1.0",
      url: "https://github.com/nhchiu/VoronMods/tree/main/Extruders/Large_Gear_Clockwork2",
      description: "Direct drive extruder for the Voron Stealthburner toolhead.",
      filament_sensor: "unknown"
    },
    {
      name: "ProtoXtruder",
      mounting_pattern: "Sherpa Mini",
      gear_type: "HGX-lite 1.0",
      url: "https://github.com/nhchiu/VoronMods/tree/main/Extruders/ProtoXtruder",
      description: "Direct drive standalone extruder, features Sherpa Mini mounting.",
      filament_sensor: "unknown"
    },
    {
      name: "ProtoXtruder 2.0",
      mounting_pattern: "Sherpa Mini",
      gear_type: "HGX-lite 2.0",
      url: "https://github.com/nhchiu/3DPrinter-Designs/tree/main/ProtoXtruder_2.0",
      description: "Direct drive standalone extruder, variant of ProtoXtruder for HGX-lite 2.0 gears.",
      filament_sensor: "unknown"
    },
    {
      name: "StealthProtoXtruder 2.0",
      mounting_pattern: "Sherpa Mini",
      gear_type: "HGX-lite 2.0",
      url: "https://www.printables.com/model/1372682-stealthprotoxtruder-20",
      description: "Direct drive standalone extruder, variant of ProtoXtruder 2.0, features HGX-lite 2.0 gears, minor improvements and a low-poly design.",
      filament_sensor: "unknown"
    },
    {
      name: "ProtoXtruder NX",
      mounting_pattern: "Sherpa Mini",
      gear_type: "HGX-lite 1.0",
      url: "https://www.printables.com/model/1020272-protoxtruder-nx",
      description: "Direct drive standalone extruder, variant of ProtoXtruder, features a latch arm based on the Prusa Nextruder design.",
      filament_sensor: "unknown"
    },
    {
      name: "Papilio",
      mounting_pattern: "other",
      gear_type: "HGX-lite 1.0",
      url: "https://github.com/kevinakasam/Papilio-Belt-Extruder",
      description: "Direct drive standalone extruder, features using belts rather than gears to push filament.",
      filament_sensor: "unknown"
    },
    {
      name: "Papilio Lite",
      mounting_pattern: "Sherpa Mini",
      gear_type: "HGX-lite 1.0",
      url: "https://github.com/kevinakasam/Papilio-Belt-Extruder",
      description: "Direct drive standalone extruder, features using belts rather than gears to push filament.",
      filament_sensor: "unknown"
    },
    {
      name: "Tsunami",
      mounting_pattern: "other",
      gear_type: "HGX-lite 1.0",
      url: "https://www.printables.com/model/1175316-beta-tsunami-extruder",
      description: "Direct drive standalone extruder, features dual drive gears.",
      filament_sensor: "unknown"
    },
    {
      name: "Galileo 2",
      mounting_pattern: "Stealthburner",
      gear_type: "Galileo",
      url: "https://github.com/JaredC01/Galileo2/tree/main/galileo2_extruder",
      description: "Direct drive extruder for the Voron Stealthburner toolhead.",
      filament_sensor: "unknown"
    },
    {
      name: "Galileo 2 Standalone",
      mounting_pattern: "other",
      gear_type: "Galileo",
      url: "https://github.com/JaredC01/Galileo2/tree/main/galileo2_standalone",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Wristwatch G2",
      mounting_pattern: "Orbiter 2",
      gear_type: "Galileo",
      url: "https://github.com/tetsu97/WristWatch-G2-Extruder",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Escapement",
      mounting_pattern: "other",
      gear_type: "Galileo",
      url: "https://github.com/Armchair-Heavy-Industries/Escapement",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Micro-cosm",
      mounting_pattern: "other",
      gear_type: "Galileo",
      url: "https://github.com/Okanoa/micro-cosm",
      description: "Direct drive standalone extruder.",
      filament_sensor: "unknown"
    },
    {
      name: "Orbiter 2",
      mounting_pattern: "Orbiter 2",
      gear_type: "other",
      url: "https://www.orbiterprojects.com/orbiter-v2-0/",
      description: "Direct drive standalone extruder, features a planetary gearbox, superceded by Orbiter 2.5.",
      category: "Pre-Assembled Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Orbiter 2.5",
      mounting_pattern: "Orbiter 2",
      gear_type: "other",
      url: "https://www.orbiterprojects.com/orbiter-v2-5/",
      description: "Direct drive standalone extruder, features a planetary gearbox.",
      category: "Pre-Assembled Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "BMG",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://www.bondtech.se/product/bmg-extruder/",
      description: "Direct drive standalone extruder.",
      category: "Pre-Assembled Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "LGX-Lite",
      mounting_pattern: "LGX-lite",
      gear_type: "other",
      url: "https://www.bondtech.se/product/lgx-lite-v2-large-gears-extruder-no-motor/",
      description: "Direct drive standalone extruder, features large drive gears.",
      category: "Pre-Assembled Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "RoundAbout",
      mounting_pattern: "Sherpa Mini",
      gear_type: "other",
      url: "https://github.com/chirpy2605/voron/tree/main/general/RoundAbout",
      description: "Direct drive standalone extruder, features a planetary gearbox.",
      category: "Other Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "RoundTrip",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://github.com/chirpy2605/voron/tree/main/general/RoundTrip",
      description: "Direct drive standalone extruder, features support for Orbiter V1.5, LGX lite, TBG lite, and Orbiter V2 gears.",
      category: "Other Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Lowboi Mk4",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://github.com/Shaunuss/Lowboi-Mk4",
      description: "Direct drive standalone extruder, features a worm gear design with Creality K1 gears.",
      category: "Other Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Waterweel",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://github.com/abigexplosion/WATERHWEEL-EXTRUDER/tree/main",
      description: "Direct drive standalone extruder, features a worm gear design with Bambu X/P series gears.",
      category: "Other Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Orbiter 1.5",
      mounting_pattern: "Orbiter",
      gear_type: "other",
      url: "https://www.orbiterprojects.com/orbiter-1-5/",
      description: "Direct drive standalone extruder, features a planetary gearbox, superceded by Orbiter 2/2.5.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Galileo 1",
      mounting_pattern: "Afterburner",
      gear_type: "other",
      url: "https://github.com/JaredC01/Galileo",
      description: "Direct drive extruder for the Voron Afterburner toolhead, features a planetary gearbox, superceded by Galileo 2.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Stargazer",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://www.printables.com/model/245121-stargazer-v1",
      description: "Direct drive standalone extruder, features planetary gearbox.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Double Folded Ascender",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://github.com/Annex-Engineering/Folded_Ascender-Extruder",
      description: "Direct drive standalone extruder, features a worm gear design.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "NF-Cannon",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://github.com/Mellow-3D/NF-Cannon",
      description: "Direct drive standalone extruder, features a worm gear design.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Zesty-Nimble V3",
      mounting_pattern: "other",
      gear_type: "other",
      url: "https://zestytechnology.readthedocs.io/en/latest/nimble/nimblev3/index.html",
      description: "Worm gear extruder.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Titania",
      mounting_pattern: "Sherpa Mini",
      gear_type: "other",
      url: "https://www.printables.com/model/729823-titania-extruder-with-e3d-titan-gears-made-in-sher",
      description: "Direct drive standalone extruder, variant of Sherpa Mini, features E3D titan gears.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    },
    {
      name: "Cyclops",
      mounting_pattern: "Creality K1",
      gear_type: "other",
      url: "https://github.com/D3vil-Design/Cyclops-Extruder",
      description: "Direct drive extruder for the Creality K1 toolhead.",
      category: "Legacy Extruders",
      filament_sensor: "unknown"
    }
  ]
};

// src/data/hotends.json
var hotends_default = {
  hotends: [
    {
      name: "E3D V6",
      mounting_pattern: [
        "Groove"
      ],
      length: "59.93mm",
      meltzone_length: "16.5mm",
      hotend_type: "SF",
      flow_rate: "~12 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://e3d-online.com/products/v6-all-metal-hotend",
      description: "The OG hotend, supports V6 groove mount, V6 nozzles, max flow ~12 mm3/s."
    },
    {
      name: "Revo",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "58mm",
      meltzone_length: "18.2mm",
      hotend_type: "SF",
      flow_rate: "~12 mm3/s",
      nozzle_compatibility: [
        "Revo"
      ],
      url: "https://e3d-online.com/pages/revo-configurator",
      description: "The new and improved V6 hotend, has variants for V6 groove mount and Voron mount, features quick cold swap nozzles, ceramic heater, max flow ~12 mm3/s."
    },
    {
      name: "Panda Revo",
      mounting_pattern: [
        "Bambu"
      ],
      length: "44.15mm",
      meltzone_length: "18.2mm",
      hotend_type: "SF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Revo"
      ],
      url: "https://biqu.equipment/products/biqu-panda-hotend",
      description: "Variant of E3D Revo, supports Bambu mounting."
    },
    {
      name: "Dragon SF",
      mounting_pattern: [
        "Screws"
      ],
      length: "45.5mm",
      meltzone_length: "26mm",
      hotend_type: "SF",
      flow_rate: "~15 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/dragon-hotend-st",
      description: "V6 style hotend, introduced dragon mounting, one-handed nozzle changes, max flow ~15 mm3/s."
    },
    {
      name: "Dragonfly",
      mounting_pattern: [
        "Groove",
        "other"
      ],
      length: "45.6mm",
      meltzone_length: "18.5mm",
      hotend_type: "SF",
      flow_rate: "~15 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/dragonfly-hotend-bms",
      description: "V6/Ender style hotend, supports groove or Ender mounting, max flow ~15 mm3/s."
    },
    {
      name: "CHC",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "58mm",
      meltzone_length: "20.2mm",
      hotend_type: "SF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://trianglelab.net/products/tchc-td6s-hotend",
      description: "V6 style hotend, supports groove mount, ceramic heater, single or two-piece nozzle and throat."
    },
    {
      name: "NF-Crazy",
      mounting_pattern: [
        "Groove",
        "Volcano"
      ],
      length: "49mm",
      meltzone_length: "unknown",
      hotend_type: "SF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://3dmellow.com/products/nf-crazy-v2-hotend",
      description: "Ender style hotend, supports Ender or V6 mounting, V6 or V6 Volcano nozzles, one-handed nozzle changes."
    },
    {
      name: "NF-Crazy Volcano",
      mounting_pattern: [
        "Groove",
        "other"
      ],
      length: "unknowm",
      meltzone_length: "34.5mm",
      hotend_type: "HF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Volcano"
      ],
      url: "https://3dmellow.com/products/nf-crazy-v2-hotend",
      description: "Ender style hotend, supports Ender or V6 mounting, V6 or V6 Volcano nozzles, one-handed nozzle changes."
    },
    {
      name: "Mosquito",
      mounting_pattern: [
        "other"
      ],
      length: "41mm",
      meltzone_length: "18.9mm",
      hotend_type: "SF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.sliceengineering.com/en-ca/products/the-mosquito-hotend",
      description: "Premium hotend by Slice Engineering."
    },
    {
      name: "Rapido HF",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "50.8mm",
      meltzone_length: "33.0mm",
      hotend_type: "HF",
      flow_rate: "~25 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/rapido-hotend",
      description: "V6 style hotend, supports groove or Dragon mounting, thermocouples, one-handed nozzle changes, max flow ~25 mm3/s, more with ultra high adapter."
    },
    {
      name: "Dragon HF",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "45.5mm",
      meltzone_length: "26mm",
      hotend_type: "HF",
      flow_rate: "~22 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/dragon-hotend-hf",
      description: "V6 style hotend, supports groove or Dragon mount, one handed nozzle changes, max flow ~22 mm3/s."
    },
    {
      name: "Dragonfly HIC",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "unknown",
      hotend_type: "HF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Volcano"
      ],
      url: "https://www.phaetus.com/en-us/products/drangonfly-hotend-hic",
      description: "Ender style hotend, supports Ender mounting, Volcano style nozzles."
    },
    {
      name: "Red Lizard K1 HF",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "46.0mm",
      meltzone_length: "26mm",
      hotend_type: "HF",
      flow_rate: "~22 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "",
      description: "V6 style hotend, clone of Dragon HF, supports groove or Dragon mounting, one handed nozzle changes, max flow ~22 mm3/s."
    },
    {
      name: "Bambu X1/P1",
      mounting_pattern: [
        "Bambu"
      ],
      length: "44.15mm",
      meltzone_length: "24.2mm",
      hotend_type: "HF",
      flow_rate: "~22 mm3/s",
      nozzle_compatibility: [
        "Integrated"
      ],
      url: "https://us.store.bambulab.com/products/bambu-hotend-x1c",
      description: "Bambulabs hotend, supports X1/P1 style mounting, features a low price, max flow ~22 mm3/s."
    },
    {
      name: "TZ2.0",
      mounting_pattern: [
        "Groove",
        "Screws",
        "Bambu"
      ],
      length: "44.15mm",
      meltzone_length: "25.4mm",
      hotend_type: "HF",
      flow_rate: "~22 mm3/s",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://trianglelab.net/products/tz-20-hotend-for-bambu-lab-x1-p1p",
      description: "Clone hotend with Bambulabs style heater block, available in Bambulabs, V6+Dragon, or Ender style heatbreak and mounting, features V6 nozzles, one handed nozzle changes, max flow ~22 mm3/s."
    },
    {
      name: "TZ 3.0/4.0",
      mounting_pattern: [
        "Groove",
        "Screws",
        "Bambu"
      ],
      length: "45.15mm",
      meltzone_length: "24.2mm",
      hotend_type: "HF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://www.trianglelab.net/products/tz-40-hotend",
      description: "Clone hotend with Bambulabs style heater block, variant of TZ 2.0, available in Bambulabs, V6+Dragon, or Ender style heatbreak and mounting, features single-piece throat+nozzle design, one handed nozzle changes."
    },
    {
      name: "CHC Pro",
      mounting_pattern: [
        "Screws",
        "Groove"
      ],
      length: "63mm",
      meltzone_length: "27.2mm",
      hotend_type: "HF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Volcano"
      ],
      url: "https://trianglelab.net/products/chc-pro-hotend",
      description: "V6 style hotend, supports groove mount, ceramic heater, variant of CHC hotend with extended ceramic heater."
    },
    {
      name: "INo Trident",
      mounting_pattern: [
        "Screws"
      ],
      length: "61.5mm",
      meltzone_length: "17mm",
      hotend_type: "HF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://dev.plasmics.com/inotrident",
      description: "V6 style hotend, supports dragon mounting, induction heater, custom nozzles."
    },
    {
      name: "Rapido UHF",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "59.2mm",
      meltzone_length: "33.0mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/rapido-plus-hotend",
      description: "V6 style hotend, variant of Rapido, supports groove or Dragon mounting, thermocouples, ceramic heater, one handed nozzle changes."
    },
    {
      name: "Fluxheat",
      mounting_pattern: [
        "Groove",
        "Screws",
        "Bambu"
      ],
      length: "47.2mm",
      meltzone_length: "unknown",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.aliexpress.us/item/3256809993234845.html",
      description: "V6 or Bambu style hotend, Dragon or Bambu mounting, one handed nozzle changes, support for an integrated nozzle probe, V6 nozzles."
    },
    {
      name: "Heatcore",
      mounting_pattern: [
        "Screws"
      ],
      length: "65.0mm",
      meltzone_length: "unknown",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://3dmellow.com/products/mellow-heatcore-4-ultra high-alps-hotend-smart-leveling-nozzle-probe-for-voron-blv-vzbot-hevort-diy-3d-printer-high-flow-and-high-speed",
      description: "V6 style hotend, Dragon mounting, one handed nozzle changes, support for an integrated nozzle probe, V6 nozzles."
    },
    {
      name: "Conch",
      mounting_pattern: [
        "Bambu",
        "other"
      ],
      length: "unknown",
      meltzone_length: "unknown",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/conch-hotend-m6-x1-p1-series",
      description: "Hotend in Bambulabs or AD5M style, ceramic heater, one-handed nozzle changes, V6 nozzles."
    },
    {
      name: "Dragon UHF",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "53.7mm",
      meltzone_length: "32.8mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/dragon-hotend-%C2%AE-ultra high",
      description: "V6 style hotend, variant of Dragon HF, supports groove or Dragon mounting, ceramic heater, one handed nozzle changes."
    },
    {
      name: "Dragon Ace",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "52mm",
      meltzone_length: "29mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://www.phaetus.com/en-us/products/dragon-hotend-%C2%AE-ultra high",
      description: "V6 style hotend, variant of Dragon ultra high, supports groove or Dragon mount, ceramic heater, thermocouples, V6 nozzles."
    },
    {
      name: "Dragon Ace Volcano",
      mounting_pattern: [
        "Groove",
        "Screws"
      ],
      length: "60.6mm",
      meltzone_length: "37.6mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Volcano"
      ],
      url: "https://trianglelab.net/products/dragon-ace-hotend",
      description: "V6 style hotend, variant of Dragon ultra high, supports groove or Dragon mount, ceramic heater, thermocouples, V6 Volcano nozzles."
    },
    {
      name: "Red Lizard K1 UHF",
      mounting_pattern: [
        "Screws"
      ],
      length: "53.7mm",
      meltzone_length: "27.7mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "",
      description: "V6 style hotend, high flow variant of Red Lizard K1."
    },
    {
      name: "V9",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "40.5mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://www.v9hotend.com",
      description: "Ultra high flow hotend."
    },
    {
      name: "Lancer",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "43mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://peopoly.net/products/magneto-x-lancer-melt-zone",
      description: "Ultra high flow hotend by Peopoly."
    },
    {
      name: "Tricorn",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "45mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://excit3dprints.com/tricorn-hotend",
      description: "Ultra high flow hotend."
    },
    {
      name: "Takoto HE",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "56.5mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://advanced3dprinting.com/products/takoto-he50",
      description: "Ultra high flow hotend."
    },
    {
      name: "CHC XL",
      mounting_pattern: [
        "Screws"
      ],
      length: "69.3mm",
      meltzone_length: "51.5mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://trianglelab.net/products/chc-xl-hotend",
      description: "Dragon mount hotend, extreme high flow, wear resistant, air or water cooled."
    },
    {
      name: "STD6-LC",
      mounting_pattern: [
        "Groove"
      ],
      length: "unknown",
      meltzone_length: "69mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6",
        "Volcano"
      ],
      url: "https://www.trianglelab.net/products/std6-lc",
      description: "V6 style hotend, supports screw or groove mounting, ceramic heater, water cooling, V6 or V6 Volcano nozzles."
    },
    {
      name: "Chube",
      mounting_pattern: [
        "Screws"
      ],
      length: "60mm",
      meltzone_length: "39.5mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://chubehotend.com",
      description: "V6 style hotend, supports dragon mounting, V6 nozzles."
    },
    {
      name: "Goliath",
      mounting_pattern: [
        "Screws"
      ],
      length: "69.21mm",
      meltzone_length: "49mm",
      hotend_type: "UHF",
      flow_rate: "Unknown",
      nozzle_compatibility: [
        "V6"
      ],
      url: "https://github.com/VzBoT3D/Goliath",
      description: "V6 style hotend, supports dragon mounting, nichrome wire heater, thermocouples, water or air cooling."
    },
    {
      name: "Vesuvius",
      mounting_pattern: [
        "other"
      ],
      length: "unknown",
      meltzone_length: "92mm",
      hotend_type: "UHF",
      flow_rate: "~150 mm3/s",
      nozzle_compatibility: [
        "Other"
      ],
      url: "https://www.causalitymfg.com/vesuvius",
      description: "Custom hotend, extreme flow rates targeting ~150mm3/s."
    }
  ]
};

// src/data/probes.json
var probes_default = {
  probes: [
    {
      name: "BLTouch",
      type: "touch",
      url: "https://www.antclabs.com/bltouch",
      description: "The OG probe, wide-compatibility, fragile pin, best suited for unenclosed printers."
    },
    {
      name: "3DTouch",
      type: "touch",
      url: "https://trianglelab.net/products/3d-touch",
      description: "Clone of BLTouch, available from many vendors, lower cost alternative."
    },
    {
      name: "CRTouch",
      type: "touch",
      url: "https://www.creality3dofficial.com/products/creality-cr-touch",
      description: "Variant of BLTouch, features a metal probe pin."
    },
    {
      name: "MicroProbe",
      type: "touch",
      url: "https://github.com/bigtreetech/MicroProbe",
      description: "Lightweight touch probe."
    },
    {
      name: "Omron",
      type: "touchless",
      url: "https://west3d.com/products/omron-tl-q5m-inductive-probe",
      description: "An inductive probe often fit within the Voron x-carriage, clones available with varying quality."
    },
    {
      name: "SuperPINDA",
      type: "touchless",
      url: "https://www.prusa3d.com/product/superpinda/",
      description: "The classic Prusa inductive probe fit on many of their classic printers, similar probes available from other vendors."
    },
    {
      name: "Klicky",
      type: "dockable",
      url: "https://github.com/jlas1/Klicky-Probe",
      description: "Dockable microswitch probe, features a mouse switch for probe, wide printer support, supports fixed docking."
    },
    {
      name: "Klicky PCB",
      type: "dockable",
      url: "https://github.com/tanaes/whopping_Voron_mods/tree/main/pcb_klicky",
      description: "Dockable microswitch probe, variant of Klicky with a PCB main switch body."
    },
    {
      name: "Quickdraw",
      type: "dockable",
      url: "https://github.com/Annex-Engineering/Quickdraw_Probe",
      description: "Dockable microswitch probe."
    },
    {
      name: "Euclid",
      type: "dockable",
      url: "https://euclidprobe.github.io/",
      description: "Dockable microswitch probe, PCB main body."
    },
    {
      name: "ZeroClick",
      type: "dockable",
      url: "https://github.com/zruncho3d/ZeroClick",
      description: "Dockable microswitch probe for Voron V0."
    },
    {
      name: "Unklicky",
      type: "dockable",
      url: "https://github.com/majarspeed/Unklicky",
      description: "Dockable probe, high sensitivity conductive probe, many variations for different applications."
    },
    {
      name: "SlideSwipe",
      type: "dockable",
      url: "https://github.com/chestwood96/SlideSwipe",
      description: "Dockable microswitch probe for Voron V0, features a servo dock."
    },
    {
      name: "SideSwipe",
      type: "dockable",
      url: "https://github.com/oldfar-t/Side-Swipe-Magnetic-Probe",
      description: "Dockable microswitch probe for Voron V0, features a servo dock."
    },
    {
      name: "TAP",
      type: "nozzle",
      url: "https://github.com/VoronDesign/Voron-Tap",
      description: "Probe designed to allow the X carriage to move in the Z direction for probing, uses a microswitch or optical sensor, often available in CNC aluminum or carbon fiber variants."
    },
    {
      name: "Boop",
      type: "nozzle",
      url: "https://github.com/PrintersForAnts/Boop",
      description: "Similar to TAP, designed for Printers for Ants, features an optical sensor."
    },
    {
      name: "PZ",
      type: "nozzle",
      url: "https://e3d-online.com/products/pz-probe",
      description: "Nozzle probe featuring a piezoelectric sensor attached to the Revo Voron hotend."
    },
    {
      name: "ALPS",
      type: "nozzle",
      url: "https://3dmellow.com/products/mellow-heatcore-4-uhf-alps-hotend-smart-leveling-nozzle-probe-for-voron-blv-vzbot-hevort-diy-3d-printer-high-flow-and-high-speed",
      description: "Nozzle probe featuring a pressure sensor attached to a Mellow HeatCore, FluxHeat, or V6 hotend."
    },
    {
      name: "Z-Probe Membrane",
      type: "nozzle",
      url: "https://trianglelab.net/products/z-probe-1",
      description: "Nozzle probe featuring a membrane sensor, must be removed after probing."
    },
    {
      name: "Z-Probe Piezo20",
      type: "nozzle",
      url: "https://trianglelab.net/products/z-probe-2",
      description: "Piezo nozzle probe designed for V6-style hotends."
    },
    {
      name: "Cartographer",
      type: "bed_scanner",
      url: "https://cartographer3d.com/",
      description: "Eddy current sensor bed scanner, supports CANbus or USB, built-in ADXL345."
    },
    {
      name: "Beacon",
      type: "bed_scanner",
      url: "https://beacon3d.com/",
      description: "Eddy current sensor bed scanner, supports USB."
    },
    {
      name: "Eddy",
      type: "bed_scanner",
      url: "https://github.com/bigtreetech/Eddy",
      description: "Eddy current sensor bed scanner, supports USB, CANBus, or I2C, fits within the Voron X-carriage using the Omron inductive probe mount."
    },
    {
      name: "Mellow Scanner",
      type: "bed_scanner",
      url: "https://3dmellow.com/products/mellow-fly-sht36-v3-klipper-compatible-3d-printer-klipper-rrf-canbus-rs232-board-max-plus-ldc1612-eddy-for-nema14-17-motor",
      description: "Eddy current sensor bed scanner, typically paired with the Mellow toolhead board."
    },
    {
      name: "BD Sensor",
      type: "bed_scanner",
      url: "https://mellow.klipper.cn/en/docs/ProductDoc/ExtensionBoard/bd-sensor/",
      description: "Eddy current sensor bed probe, supports USB, must be removed after probing."
    },
    {
      name: "IDM Scanner",
      type: "bed_scanner",
      url: "https://github.com/Killajoedotcom/IDM",
      description: "Eddy current bed sensor, supports CANbus."
    }
  ]
};

// src/data/toolheads.json
var toolheads_default = {
  toolheads: [
    {
      name: "A4T",
      extruders: [
        "Sherpa Mini",
        "LGX-lite",
        "Orbiter 2",
        "Wristwatch G2",
        "Wristwatch BMG",
        "Vz HextrudORT"
      ],
      probe: [
        "Tap",
        "Klicky",
        "Eddy",
        "Omron"
      ],
      hotend: [
        "Bambu X1/P1",
        "Dragon HF",
        "Dragon UHF",
        "Dragon Ace",
        "NF-Crazy",
        "Rapido HF",
        "Rapido UHF",
        "Revo",
        "TZ2.0",
        "Chube",
        "NF-Crazy"
      ],
      url: "https://github.com/Armchair-Heavy-Industries/A4T",
      description: "Lightweight toolhead for Voron 2.4 and Trident, accepts standalone extruders.",
      title: "A4T",
      image: "/A4T_render.webp",
      category: "Full Size Printers",
      configurator: true,
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "native",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Afterburner",
      extruders: [
        "Clockwork 1"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/VoronDesign/Voron-Afterburner",
      description: "Toolhead for Voron 2.4 R1 and Trident, designed for Clockwork1 extruder.",
      category: "Full Size Printers",
      configurator: false,
      title: "Afterburner",
      image: "",
      hotend_fan: "4010",
      part_cooling_fan: "4020",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Angry",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/Shamoon78/Angry_Tool_Head",
      description: "Toolhead for Monolith gantry.",
      category: "Full Size Printers",
      configurator: false,
      title: "Angry",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "AntHead",
      extruders: [
        "Sherpa Mini",
        "Wristwatch BMG",
        "Wristwatch G2",
        "Galileo 2",
        "Orbiter 2"
      ],
      probe: [
        "Beacon",
        "Cartographer"
      ],
      hotend: [
        "Bambu X1/P1",
        "Dragon Ace",
        "Revo",
        "Dragon HF",
        "Dragon UHF",
        "Rapido HF"
      ],
      url: "https://github.com/PrintersForAnts/AntHead",
      description: "Lightweight toolhead for Printers for Ants.",
      title: "AntHead",
      image: "/Anthead_render.webp",
      category: "Printers for Ants",
      configurator: true,
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Archetype",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "HextrudORT"
      ],
      probe: [
        "Klicky PCB",
        "Klicky",
        "Euclid",
        "Beacon"
      ],
      hotend: [
        "Chube",
        "Goliath",
        "Rapido HF",
        "Rapido UHF"
      ],
      url: "https://github.com/Armchair-Heavy-Industries/Archetype",
      description: "Toolhead for Voron 2.4 and Trident, features support for varying fan types.",
      category: "Full Size Printers",
      configurator: true,
      title: "Archetype",
      image: "/Archetype.webp",
      hotend_fan: "2510",
      part_cooling_fan: [
        "5020",
        "4010",
        "3515",
        "5015",
        "CPAP"
      ],
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Banantis",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/jakub874/Banantis",
      description: "Toolhead for Monolith gantry, variant of Mantis.",
      category: "Full Size Printers",
      configurator: false,
      title: "Banantis",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Calamity",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/lukeslaboratory/Calamity-Toolhead",
      description: "Toolhead for Voron 2.4 and Trident.",
      category: "Full Size Printers",
      configurator: false,
      title: "Calamity",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Crown Cooler",
      extruders: [
        "Sherpa Mini",
        "LGX-lite",
        "Orbiter 2",
        "HextrudORT",
        "Clockwork1",
        "Clockwork2"
      ],
      probe: [
        "Klicky",
        "Euclid"
      ],
      hotend: [
        "Rapido UHF",
        "Dragon SF",
        "Rapido HF",
        "Dragon HF"
      ],
      url: "https://github.com/sneakytreesnake/CrownCooler",
      description: "Toolhead for Voron 2.4 and Trident, lightweight, features support for the Stealthburner X carriage, CPAP, standalone extruders, and UHF hotends.",
      category: "Full Size Printers",
      configurator: true,
      title: "Crown Cooler",
      image: "/crown_cooler.webp",
      hotend_fan: "4028",
      part_cooling_fan: "CPAP",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "DK's Monolith",
      extruders: [
        "Sherpa Micro"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/Kizime123/DKs-Monolith-Toolhead",
      description: "Toolhead for Monolith gantry.",
      category: "Full Size Printers",
      configurator: false,
      title: "DK's Monolith",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Dragon Brick",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/robwaldhauser/Dragon-Brick",
      description: "Toolhead designed for Madmax toolchanger, variant of Dragon Burner, features a compact size and a front mounted PCB.",
      category: "Printers for Ants",
      configurator: false,
      title: "Dragon Brick",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Dragon Burner",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "Sailfin",
        "Sharkfin",
        "E3D Roto Vitamins",
        "LGX-lite",
        "Orbiter 2",
        "Orbiter 1.5",
        "Vz HextrudORT",
        "Double Folded Ascender",
        "RoundTrip",
        "Galileo 2 Standalone",
        "Wristwatch BMG",
        "Wristwatch G2",
        "Papilio Lite"
      ],
      probe: [
        "SlideSwipe",
        "Unklicky",
        "ZeroClick",
        "Klicky",
        "TAP",
        "Boop"
      ],
      hotend: [
        "Dragon SF",
        "Dragon HF",
        "Dragonfly",
        "Revo",
        "Rapido HF",
        "NF-Crazy",
        "Creality Spider Pro",
        "Bambu X1/P1",
        "TaiChi",
        "E3D V6",
        "Red Lizard K1 HF",
        "Red Lizard K1 UHF",
        "Dragon Ace",
        "Phaetus NeXt G HF",
        "Phaetus NeXt G UHF",
        "Chube"
      ],
      url: "https://github.com/chirpy2605/voron/tree/main/V0/Dragon_Burner",
      description: "Lightweight toolhead for Printers for Ants.",
      title: "Dragon Burner",
      image: "/dragonburner.webp",
      category: "Printers for Ants",
      configurator: true,
      hotend_fan: [
        "3010",
        "2510"
      ],
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "EVA",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://main.eva-3d.page/",
      description: "Modular toolhead for MGN12/15 CoreXY printers, designed for Ratrig.",
      category: "Full Size Printers",
      configurator: false,
      title: "EVA",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "unknown"
    },
    {
      name: "Fibre Steam",
      extruders: [
        "Galileo 2"
      ],
      probe: "Beacon",
      hotend: [
        "Rapido UHF",
        "Dragon UHF",
        "Rapido HF",
        "Goliath"
      ],
      url: "https://github.com/tristanmcpherson/fibresteam_wiki/tree/main",
      description: "Toolhead for Monolith gantry.",
      category: "Full Size Printers",
      configurator: false,
      title: "Fibre Steam",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Fibre Stream",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/1219174-fiberstream-toolhead",
      description: "Toolhead for Monolith gantry, variant of Fibre Steam with QOL improvements.",
      category: "Full Size Printers",
      configurator: false,
      title: "Fibre Stream",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Gustav",
      extruders: [
        "Double Folded Ascender",
        "Wristwatch G2",
        "Wristwatch BMG",
        "Micro-cosm"
      ],
      probe: [
        "Beacon"
      ],
      hotend: [
        "Dragon Ace"
      ],
      url: "https://github.com/RCI-Nicket/Gustav-Railway-Toolhead",
      description: "Toolhead for Monolith gantry, features support for CPAP, UHF hotends, and beacon probe.",
      category: "Full Size Printers",
      configurator: false,
      title: "Gustav",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Hypernova",
      extruders: [
        "Sherpa Mini",
        "LGX-lite",
        "Orbiter 2",
        "Galileo 2 Standalone",
        "Wristwatch G2",
        "Wristwatch BMG"
      ],
      probe: [
        "Beacon",
        "Cartographer",
        "Klicky"
      ],
      hotend: [
        "TZ2.0",
        "Dragon Ace",
        "Red Lizard K1 UHF",
        "Rapido HF",
        "Rapido UHF",
        "Red Lizard K1 HF",
        "Red Lizard K1 UHF",
        "Chube Air"
      ],
      url: "https://hyperdrivedesign.com/products/Hypernova/hypernova/#hypernova-printhead",
      description: "Toolhead for Voron 2.4 and Trident, features a CNC metal plate for rigidity, support for UHF hotends, and accepts standalone extruders.",
      category: "Full Size Printers",
      configurator: true,
      title: "Hypernova",
      image: "/hypernova.webp",
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Hypernova BSE",
      extruders: [
        "Sherpa Mini",
        "LGX-lite",
        "Orbiter 2",
        "Galileo 2 Standalone",
        "Wristwatch G2",
        "Wristwatch BMG"
      ],
      probe: [
        "Beacon",
        "Cartographer"
      ],
      hotend: [
        "Rapido UHF",
        "Rapido HF",
        "TZ2.0",
        "Dragon Ace",
        "Red Lizard K1 UHF",
        "Chube Air"
      ],
      url: "https://hyperdrivedesign.com/products/Hypernova/hypernova_bedsligers/",
      description: "Toolhead for Switchwire/Enderwire, features a CNC metal plate for rigidity, support for UHF hotends, and accepts standalone extruders.",
      category: "Full Size Printers",
      configurator: true,
      title: "Hypernova BSE",
      image: "/hypernova_bse.webp",
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: [
        "Switchwire",
        "Enderwire"
      ],
      belt_path: "Voron coreXZ"
    },
    {
      name: "Jabberwocky",
      extruders: "other",
      probe: "unknown",
      hotend: "Conch",
      url: "https://github.com/kinematicdigit/Jabberwocky",
      description: "Toolhead for Voron 2.4 and Trident, features support for filament changers.",
      category: "Full Size Printers",
      configurator: true,
      title: "Jabberwocky",
      image: "/Jabberwocky.webp",
      hotend_fan: "2510",
      part_cooling_fan: "3010",
      filament_cutter: "native",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Mailbox",
      extruders: [
        "LGX-lite",
        "Orbiter 1.5",
        "RoundHouse",
        "RoundTrip",
        "RoundAbout"
      ],
      probe: [
        "SlideSwipe",
        "ZeroClick",
        "Unklicky"
      ],
      hotend: [
        "Revo",
        "Dragon SF",
        "Dragon HF",
        "Dragonfly",
        "Rapido HF",
        "Mellow Crazy Dragon",
        "Dragon UHF",
        "NF-Crazy"
      ],
      url: "https://github.com/chirpy2605/voron/blob/main/V0/Mailbox",
      description: "Toolhead for Voron 0, features support for enhanced 5015 fan cooling.",
      category: "Printers for Ants",
      configurator: false,
      title: "Mailbox",
      image: "",
      hotend_fan: "3010",
      part_cooling_fan: "5015",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Mantis",
      extruders: [
        "Sherpa Mini",
        "Orbiter 2",
        "Vz HextrudORT",
        "Double Folded Ascender",
        "LGX-Lite"
      ],
      probe: [
        "TAP",
        "Beacon",
        "Klicky PCB",
        "Klicky",
        "Euclid"
      ],
      hotend: [
        "Rapido HF",
        "Dragon SF",
        "Dragon HF",
        "Revo",
        "Bambu X1/P1",
        "Dragon UHF",
        "NF-Crazy"
      ],
      url: "https://github.com/Armchair-Heavy-Industries/Mantis-Xol",
      description: "Lightweight toolhead for Voron 2.4 and Trident, accepts standalone extruders.",
      category: "Full Size Printers",
      configurator: true,
      title: "Mantis",
      image: "/mantis.webp",
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Micro Saul2",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/524765-micro-saul2",
      description: "Toolhead for Voron V0.2, features support for dual 4010 fans and the mellow NF crazy hotend.",
      category: "Printers for Ants",
      configurator: false,
      title: "Micro Saul2",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Mini-Afterburner",
      extruders: [
        "NA"
      ],
      probe: "ZeroClick",
      hotend: [
        "Mosquito",
        "Dragon",
        "Dragonfly"
      ],
      url: "https://github.com/VoronDesign/Voron-0/tree/Voron0.1",
      description: "Toolhead for Voron V0.1.",
      category: "Printers for Ants",
      configurator: false,
      title: "Mini-Afterburner",
      image: "",
      hotend_fan: "3007",
      part_cooling_fan: "3010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Mini-Aftersherpa",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "Sailfin",
        "Orbiter 1.5",
        "Orbiter 2"
      ],
      probe: "ZeroClick",
      hotend: [
        "Dragon HF",
        "Dragonfly",
        "E3D V6",
        "Mosquito",
        "NF-Crazy",
        "Revo"
      ],
      url: "https://github.com/PrintersForAnts/Mini-AfterSherpa",
      description: "Toolhead for Voron V0.1, adds support for standalone extruders.",
      category: "Printers for Ants",
      configurator: false,
      title: "Mini-Aftersherpa",
      image: "",
      hotend_fan: "3007",
      part_cooling_fan: "3010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Mini-Stealthburner",
      extruders: [
        "NA"
      ],
      probe: "ZeroClick",
      hotend: [
        "Revo",
        "Dragon",
        "Dragonfly",
        "DropEffect XG",
        "Creality Spider Pro"
      ],
      url: "https://github.com/VoronDesign/Voron-0/tree/Voron0.2r1",
      description: "Toolhead for Voron V0.2.",
      category: "Printers for Ants",
      configurator: false,
      title: "Mini-Stealthburner",
      image: "",
      hotend_fan: "3007",
      part_cooling_fan: "3010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Mini-Stealthdort",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "Sailfin",
        "Sharkfin",
        "Orbiter 1.5",
        "Orbiter 2",
        "LGX-Lite",
        "HextrudORT"
      ],
      probe: [
        "Klicky",
        "ZeroClick"
      ],
      hotend: [
        "Dragon HF",
        "Dragonfly",
        "Mosquito",
        "Revo",
        "Mellow NF-Zone"
      ],
      url: "https://github.com/nhchiu/VoronMods/tree/main/V0/Toolhead",
      description: "Toolhead for Voron V0, features support for standalone extruders.",
      category: "Printers for Ants",
      configurator: true,
      title: "Mini-Stealthdort",
      image: "/Mini-Stealthdort.webp",
      hotend_fan: [
        "3007",
        "3010"
      ],
      part_cooling_fan: "3010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Omniburner",
      extruders: [
        "Sherpa Mini",
        "LGX-lite",
        "Orbiter 2",
        "LGX-Lite",
        "Papilio Lite",
        "Papilio",
        "TBG Lite"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/854317-omniburner-universial-mini-stealthburner-for-most",
      description: "Toolhead for Voron V0.2, features support for standalone extruders.",
      category: "Printers for Ants",
      configurator: false,
      title: "Omniburner",
      image: "/omniburner.webp",
      hotend_fan: "3007",
      part_cooling_fan: "3010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Rapid Burner",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "Sailfin",
        "Sharkfin",
        "Orbiter 1.5",
        "Orbiter 2",
        "Vz HextrudORT",
        "LGX-Lite",
        "Double Folded Ascender",
        "RoundTrip",
        "Galileo 2 Standalone",
        "E3D Roto Vitamins"
      ],
      probe: [
        "SlideSwipe",
        "Unklicky",
        "ZeroClick",
        "Klicky",
        "Boop",
        "TAP"
      ],
      hotend: [
        "Rapido UHF",
        "Dragon UHF",
        "Goliath",
        "E3D V6"
      ],
      url: "https://github.com/chirpy2605/voron/tree/main/V0/Rapid_Burner",
      description: "Lightweight toolhead for Printers for Ants, variant of Dragonburner, features support for high flow hotends.",
      category: "Printers for Ants",
      configurator: true,
      title: "Rapid Burner",
      image: "/rapidburner.webp",
      hotend_fan: "3010",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Rainbow Dragon",
      extruders: [
        "Other"
      ],
      probe: "unknow",
      hotend: "TaiChi",
      url: "https://github.com/chirpy2605/voron/tree/main/V0/Rainbow_Dragon",
      description: "Lightweight toolhead for Printers for Ants, features support for dual filament paths.",
      category: "Printers for Ants",
      configurator: false,
      title: "Rainbow Dragon",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Reaper",
      extruders: [
        "Sherpa Mini",
        "Orbiter 2",
        "HextrudORT",
        "LGX-Lite",
        "Hummingbird",
        "Galileo 2 Standalone"
      ],
      probe: [
        "TAP",
        "Beacon",
        "Cartographer",
        "Klicky PCB"
      ],
      hotend: [
        "Dragon SF",
        "Dragon HF",
        "Dragon UHF",
        "Rapido HF",
        "Rapido UHF",
        "Revo",
        "Mosquito",
        "Bambu X1/P1",
        "Goliath"
      ],
      url: "https://github.com/APDMachine/Reaper",
      description: "Toolhead for Voron 2.4 and Trident, inspired by Mantis.",
      category: "Full Size Printers",
      configurator: true,
      title: "Reaper",
      image: "/reaper.webp",
      hotend_fan: "3010",
      part_cooling_fan: "5015",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Rx",
      extruders: [
        "other"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://makerworld.com/models/2125971",
      description: "Toolhead for Voron 2.4 and Trident, based on Bambulab A1 toolhead, features A1/H2 motor, gears, and hotend, CPAP or 5015 cooling.",
      category: "Full Size Printers",
      configurator: false,
      title: "Rx",
      image: "/RX.webp",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Spartacus",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/745412-spartacus-the-toolhead",
      description: "Toolhead for Voron 0, variant of miniAB, features superior modularity.",
      category: "Printers for Ants",
      configurator: false,
      title: "Spartacus",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Stealthburner",
      extruders: [
        "Clockwork2",
        "Large Gear Clockwork2",
        "Galileo 2"
      ],
      probe: [
        "Tap",
        "Klicky",
        "Klicky PCB",
        "Eddy",
        "Omron"
      ],
      hotend: [
        "Dragon HF",
        "Dragonfly",
        "Rapido HF",
        "Revo",
        "E3D V6",
        "Chube"
      ],
      url: "https://github.com/VoronDesign/Voron-Stealthburner",
      description: "Toolhead for Voron 2.4 R1 and Trident, designed for Clockwork2 extruder.",
      category: "Full Size Printers",
      configurator: true,
      title: "Stealthburner",
      image: "/Stealthburner.webp",
      hotend_fan: "4010",
      part_cooling_fan: "5015",
      filament_cutter: "mod",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "StovePipe",
      extruders: [
        "Orbiter 1.5",
        "Sherpa Mini",
        "RoundAbout",
        "RoundHouseBMG",
        "LGX-Lite",
        "Sailfin"
      ],
      probe: "ZeroClick",
      hotend: [
        "NF-Crazy",
        "Rapido UHF",
        "Dragon UHF",
        "Revo",
        "Dragon SF",
        "Dragonfly",
        "Rapido HF"
      ],
      url: "https://github.com/chirpy2605/voron/blob/main/V0/StovePipe",
      description: "Toolhead for Voron 0, features CPAP support.",
      category: "Printers for Ants",
      configurator: false,
      title: "StovePipe",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    },
    {
      name: "Stormbreaker",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://github.com/Gr33n5murf/Stormbreaker-Toolhead",
      description: "Toolhead for Monolith gantry.",
      category: "Full Size Printers",
      configurator: false,
      title: "Stormbreaker",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "Takeoff",
      extruders: [
        "unknown",
        "Vz HextrudORT",
        "Sherpa Micro",
        "Sherpa Mini",
        "Orbiter 2",
        "LGX-Lite",
        "Double Folded Ascender"
      ],
      probe: "Beacon",
      hotend: [
        "Lancer",
        "Chube"
      ],
      url: "https://github.com/Kizime123/Takeoff-Toolhead",
      description: "Toolhead for Voron 2.4 and Trident, features an SLM design and large 3628 fans.",
      category: "Full Size Printers",
      configurator: false,
      title: "Takeoff",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "The Yokai",
      extruders: [
        "Sherpa Mini",
        "Sherpa Micro",
        "Orbiter 2"
      ],
      probe: "unknown",
      hotend: [
        "Rapido UHF"
      ],
      url: "https://www.printables.com/model/1462701-yokai-v35-toolhead",
      description: "Toolhead for Voron 2.4 and Trident, features ultralightweight design and CPAP.",
      category: "Full Size Printers",
      configurator: true,
      title: "The Yokai",
      image: "/yokai.webp",
      hotend_fan: "3010",
      part_cooling_fan: [
        "4010",
        "CPAP"
      ],
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Trinity",
      extruders: [
        "Sherpa Mini"
      ],
      probe: "unknown",
      hotend: "Dragon Ace Volcano",
      url: "https://github.com/WV-design/Trinity-toolhead/",
      description: "High performance part cooling toolhead, features 3628 fans and 9mm belt support.",
      category: "Full Size Printers",
      configurator: false,
      title: "Trinity",
      image: "/Trinity.webp",
      hotend_fan: "2510",
      part_cooling_fan: "3628",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "unknown"
    },
    {
      name: "UAP",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/1353685-uap-v6",
      description: "Toolhead for Monolith gantry.",
      category: "Full Size Printers",
      configurator: false,
      title: "UAP",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "unknown",
      belt_path: "Monolith coreXY"
    },
    {
      name: "V4",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/1133951-v4-toolhead-ideal-for-mmu-for-sv08-and-any-voron-g",
      description: "Toolhead for Voron 2.4, Trident, and Sovol SV08, features MMU support.",
      category: "Full Size Printers",
      configurator: false,
      title: "V4",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident",
        "SV08"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "V5",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/1285962-v5-toolhead-orbiter-v225-bambulab-edition-for-sv08",
      description: "Toolhead for Voron 2.4, Trident, and Sovol SV08, features MMU support.",
      category: "Full Size Printers",
      configurator: false,
      title: "V5",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident",
        "SV08"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Xenomorph",
      extruders: [
        "unknown"
      ],
      probe: "unknown",
      hotend: "unknown",
      url: "https://www.printables.com/model/1002270-xenomorph-toolhead-for-sovol-sv08",
      description: "Toolhead for Sovol SV08 (2.4 Clone).",
      category: "Full Size Printers",
      configurator: false,
      title: "Xenomorph",
      image: "",
      hotend_fan: "unknown",
      part_cooling_fan: "unknown",
      filament_cutter: "unknown",
      printer_compatibility: "SV08",
      belt_path: "unknown"
    },
    {
      name: "Xol",
      extruders: [
        "Sherpa Mini",
        "Double Folded Ascender",
        "Galileo 2 Standalone",
        "Wristwatch G2",
        "Wristwatch BMG",
        "Escapement",
        "Vz HextrudORT",
        "LGX-Lite",
        "Orbiter 2"
      ],
      probe: [
        "Klicky PCB",
        "Klicky",
        "Beacon",
        "Euclid",
        "TAP",
        "Eddy"
      ],
      hotend: [
        "Chube",
        "Rapido HF",
        "Rapido UHF",
        "Red Lizard K1 HF",
        "Dragon UHF",
        "Dragon SF",
        "Dragon HF",
        "Dragon Ace",
        "Revo",
        "NF-Crazy"
      ],
      url: "https://github.com/Armchair-Heavy-Industries/Xol-Toolhead",
      description: "Lightweight toolhead for Voron 2.4 and Trident, accepts standalone extruders.",
      category: "Full Size Printers",
      configurator: true,
      title: "Xol",
      image: "/Xol.webp",
      hotend_fan: "2510",
      part_cooling_fan: [
        "4010",
        "CPAP"
      ],
      filament_cutter: "unknown",
      printer_compatibility: [
        "V2.4",
        "Trident"
      ],
      belt_path: "Voron coreXY"
    },
    {
      name: "Yavoth",
      extruders: [
        "Sherpa Mini",
        "Galileo 2 Standalone",
        "E3D Roto Vitamins",
        "Wristwatch BMG",
        "Wristwatch G2",
        "Sherpa Micro",
        "Sharkfin",
        "Sailfin",
        "Dragon Ace",
        "Orbiter 2"
      ],
      probe: "unknown",
      hotend: [
        "Rapido HF",
        "Revo",
        "Dragon UHF",
        "Rapido UHF",
        "Chube"
      ],
      url: "https://github.com/chirpy2605/voron/blob/main/V0/Yavoth",
      description: "Toolhead for Voron 0 with support for HF and UHF hotends.",
      category: "Printers for Ants",
      configurator: true,
      title: "Yavoth",
      image: "/yavoth.webp",
      hotend_fan: "2510",
      part_cooling_fan: "4010",
      filament_cutter: "unknown",
      printer_compatibility: "V0",
      belt_path: "Voron coreXY"
    }
  ]
};

// src/scanner.ts
var BRANCH_CANDIDATES = ["main", "master"];
var PLACEHOLDER_VALUES = /* @__PURE__ */ new Set(["unknown", "unknow", "other", "na", "n/a", "none", ""]);
var DEFAULT_DATA_SOURCE_BASE = "https://raw.githubusercontent.com/SartorialGrunt0/ToolheadBuilder/main/src/data";
var FAN_PATTERNS = [
  ["CPAP", ["\\bcpap\\b", "\\b7040\\b", "\\b70\\s*[xX]\\s*40\\b"]],
  ["5015", ["\\b5015\\b"]],
  ["4010", ["\\b4010\\b"]],
  ["4020", ["\\b4020\\b"]],
  ["4015", ["\\b4015\\b"]],
  ["3010", ["\\b3010\\b"]],
  ["3007", ["\\b3007\\b"]],
  ["2510", ["\\b2510\\b"]]
];
var PART_COOLING_CONTEXT_PATTERNS = [
  "\\bpart\\s*cool(?:ing)?\\b",
  "\\bblower\\b",
  "\\bcpap\\b",
  "\\bduct\\b",
  "\\bradial\\b"
];
var HOTEND_CONTEXT_PATTERNS = [
  "\\bhot\\s*end\\b",
  "\\bhotend\\b",
  "\\bheatsink\\b",
  "\\bheat\\s*break\\b",
  "\\baxial\\b"
];
var FILAMENT_CUTTER_PATTERNS = [
  "\\bfilament\\s*cutter\\b",
  "\\bcutter\\b.{0,24}\\bfilament\\b",
  "\\bfilament\\b.{0,24}\\bcutter\\b",
  "\\bercf\\b.{0,24}\\bcutter\\b"
];
var PRINTER_PATTERNS = [
  ["V0", ["\\bv0\\b", "\\bvoron\\s*0\\b", "\\bvoron\\s*zero\\b", "\\bv0\\.\\d\\b"]],
  ["V2.4", ["\\bv2\\.4\\b", "\\bvoron\\s*2\\.4\\b", "\\bvoron\\s*2\\b(?!\\.[^4])"]],
  ["Trident", ["\\btrident\\b", "\\bvt\\b"]],
  ["Switchwire", ["\\bswitchwire\\b"]],
  ["Enderwire", ["\\benderwire\\b"]],
  ["SV08", ["\\bsv08\\b", "\\bsovol\\s*sv08\\b"]]
];
var PRINTER_TO_BELT_PATH = {
  "V0": "Voron coreXY",
  "V2.4": "Voron coreXY",
  "Trident": "Voron coreXY",
  "Switchwire": "Voron coreXZ",
  "Enderwire": "Voron coreXZ",
  "SV08": "unknown"
};
var BELT_PATH_PATTERNS = [
  ["Monolith coreXY", ["\\bmonolith\\b"]]
];
var SCAN_FETCH_BUDGET = 33;
function sanitizeAliasSection(value) {
  if (!value || typeof value !== "object") {
    return {};
  }
  return Object.fromEntries(
    Object.entries(value).filter(([key, mapped]) => !key.startsWith("_") && typeof mapped === "string").map(([key, mapped]) => [key, mapped])
  );
}
__name(sanitizeAliasSection, "sanitizeAliasSection");
function sanitizeAliases(seed) {
  const raw = seed ?? {};
  return {
    extruders: sanitizeAliasSection(raw.extruders),
    hotends: sanitizeAliasSection(raw.hotends),
    probes: sanitizeAliasSection(raw.probes),
    boards: sanitizeAliasSection(raw.boards),
    printers: sanitizeAliasSection(raw.printers)
  };
}
__name(sanitizeAliases, "sanitizeAliases");
function cleanExtraLocations(entries) {
  if (!Array.isArray(entries)) {
    return [];
  }
  const cleaned = [];
  for (const entry of entries) {
    if (!entry || typeof entry !== "object") {
      continue;
    }
    const toolhead = String(entry.toolhead ?? "").trim();
    const url = String(entry.url ?? "").trim();
    if (toolhead && url) {
      cleaned.push({ toolhead, url });
    }
  }
  return cleaned;
}
__name(cleanExtraLocations, "cleanExtraLocations");
async function loadExtraLocations(env) {
  const stored = await env.SCANNER_STATE.get("extra-locations", "json");
  if (stored && typeof stored === "object") {
    const storedLocations = cleanExtraLocations(stored.extra_locations);
    if (storedLocations.length > 0) {
      return storedLocations;
    }
  }
  const seedLocations = cleanExtraLocations(extra_github_locations_seed_default.extra_locations);
  if (seedLocations.length > 0) {
    await saveExtraLocations(env, seedLocations);
  }
  return seedLocations;
}
__name(loadExtraLocations, "loadExtraLocations");
async function saveExtraLocations(env, locations) {
  const deduped = dedupeExtraLocations(cleanExtraLocations(locations));
  await env.SCANNER_STATE.put(
    "extra-locations",
    JSON.stringify({ extra_locations: deduped }, null, 2)
  );
}
__name(saveExtraLocations, "saveExtraLocations");
async function addExtraLocation(env, toolhead, url) {
  const normalizedToolhead = toolhead.trim();
  const normalizedUrl = url.trim();
  if (!normalizedToolhead) {
    throw new Error("Toolhead name is required.");
  }
  if (!normalizedUrl.startsWith("http://") && !normalizedUrl.startsWith("https://")) {
    throw new Error("URL must be an HTTP or HTTPS URL.");
  }
  const existing = await loadExtraLocations(env);
  const alreadyExists = existing.some(
    (entry) => entry.toolhead.toLowerCase() === normalizedToolhead.toLowerCase() && entry.url === normalizedUrl
  );
  if (alreadyExists) {
    return false;
  }
  existing.push({ toolhead: normalizedToolhead, url: normalizedUrl });
  await saveExtraLocations(env, existing);
  return true;
}
__name(addExtraLocation, "addExtraLocation");
async function removeExtraLocation(env, toolhead, url) {
  const existing = await loadExtraLocations(env);
  const filtered = existing.filter(
    (entry) => !(entry.toolhead.toLowerCase() === toolhead.trim().toLowerCase() && entry.url === url.trim())
  );
  if (filtered.length === existing.length) {
    return false;
  }
  await saveExtraLocations(env, filtered);
  return true;
}
__name(removeExtraLocation, "removeExtraLocation");
function dedupeExtraLocations(entries) {
  const seen = /* @__PURE__ */ new Set();
  return entries.filter((entry) => {
    const key = `${entry.toolhead.toLowerCase()}::${entry.url}`;
    if (seen.has(key)) {
      return false;
    }
    seen.add(key);
    return true;
  });
}
__name(dedupeExtraLocations, "dedupeExtraLocations");
function cleanBlacklist(entries) {
  if (!Array.isArray(entries)) {
    return [];
  }
  return entries.map((e) => typeof e === "string" ? e.trim() : "").filter((e) => e.length > 0);
}
__name(cleanBlacklist, "cleanBlacklist");
function dedupeBlacklist(entries) {
  const seen = /* @__PURE__ */ new Set();
  return entries.filter((e) => {
    const key = e.toLowerCase();
    if (seen.has(key)) {
      return false;
    }
    seen.add(key);
    return true;
  });
}
__name(dedupeBlacklist, "dedupeBlacklist");
async function loadBlacklist(env) {
  const stored = await env.SCANNER_STATE.get("blacklist", "json");
  if (stored && typeof stored === "object") {
    const items = cleanBlacklist(stored.blacklist);
    if (items.length > 0) {
      return items;
    }
  }
  return [];
}
__name(loadBlacklist, "loadBlacklist");
async function saveBlacklist(env, entries) {
  const deduped = dedupeBlacklist(cleanBlacklist(entries));
  await env.SCANNER_STATE.put("blacklist", JSON.stringify({ blacklist: deduped }, null, 2));
}
__name(saveBlacklist, "saveBlacklist");
async function addBlacklistEntry(env, toolhead) {
  const normalized = toolhead.trim();
  if (!normalized) {
    throw new Error("Toolhead name is required.");
  }
  const existing = await loadBlacklist(env);
  if (existing.some((e) => e.toLowerCase() === normalized.toLowerCase())) {
    return false;
  }
  existing.push(normalized);
  await saveBlacklist(env, existing);
  return true;
}
__name(addBlacklistEntry, "addBlacklistEntry");
async function removeBlacklistEntry(env, toolhead) {
  const existing = await loadBlacklist(env);
  const filtered = existing.filter((e) => e.toLowerCase() !== toolhead.trim().toLowerCase());
  if (filtered.length === existing.length) {
    return false;
  }
  await saveBlacklist(env, filtered);
  return true;
}
__name(removeBlacklistEntry, "removeBlacklistEntry");
function githubRawReadmeUrls(githubUrl) {
  const url = githubUrl.replace(/\/$/, "").replace(/\.git$/, "");
  const treeMatch = /github\.com\/([^/]+)\/([^/]+)\/(?:tree|blob)\/([^/]+)(?:\/(.*))?$/i.exec(url);
  if (treeMatch) {
    const [, owner, repo, branch, subpath] = treeMatch;
    const prefix = subpath ? `${subpath}/` : "";
    const base = `https://raw.githubusercontent.com/${owner}/${repo}/${branch}`;
    return [`${base}/${prefix}README.md`];
  }
  const repoMatch = /github\.com\/([^/]+)\/([^/]+)$/i.exec(url);
  if (repoMatch) {
    const [, owner, repo] = repoMatch;
    return BRANCH_CANDIDATES.map(
      (branch) => `https://raw.githubusercontent.com/${owner}/${repo}/${branch}/README.md`
    );
  }
  return [];
}
__name(githubRawReadmeUrls, "githubRawReadmeUrls");
function githubRawFileUrl(githubUrl) {
  const url = githubUrl.replace(/\/$/, "").replace(/\.git$/, "");
  const blobMatch = /github\.com\/([^/]+)\/([^/]+)\/blob\/([^/]+)\/(.*)$/i.exec(url);
  if (blobMatch) {
    const [, owner, repo, branch, filePath] = blobMatch;
    return `https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${filePath}`;
  }
  const treeMatch = /github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.*)$/i.exec(url);
  if (treeMatch) {
    const [, owner, repo, branch, filePath] = treeMatch;
    return `https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${filePath}`;
  }
  if (url.includes("raw.githubusercontent.com")) {
    return url;
  }
  return null;
}
__name(githubRawFileUrl, "githubRawFileUrl");
function githubContentCandidates(githubUrl, sourceFile) {
  const candidates = [];
  if (sourceFile === "extra_github_locations.json") {
    const directRaw = githubRawFileUrl(githubUrl);
    if (directRaw) {
      candidates.push(directRaw);
    }
  }
  candidates.push(...githubRawReadmeUrls(githubUrl));
  return [...new Set(candidates)];
}
__name(githubContentCandidates, "githubContentCandidates");
async function fetchText(url) {
  try {
    const response = await fetch(url, {
      headers: { "User-Agent": "ToolheadScannerWorker/1.0" },
      redirect: "follow"
    });
    if (!response.ok) {
      return null;
    }
    return await response.text();
  } catch {
    return null;
  }
}
__name(fetchText, "fetchText");
async function fetchJson(url) {
  const text = await fetchText(url);
  if (text === null) {
    return null;
  }
  try {
    return JSON.parse(text);
  } catch {
    return null;
  }
}
__name(fetchJson, "fetchJson");
function hasNamedArray(value, key) {
  if (!value || typeof value !== "object") {
    return false;
  }
  const entries = value[key];
  return Array.isArray(entries);
}
__name(hasNamedArray, "hasNamedArray");
async function loadReferenceData(log, dataSourceBase, useLocalOnly = false) {
  if (useLocalOnly) {
    log("Using bundled seed data (local-only mode)");
    return {
      extruders: extruders_default.extruders,
      hotends: hotends_default.hotends,
      probes: probes_default.probes,
      toolheads: toolheads_default.toolheads
    };
  }
  const base = dataSourceBase || DEFAULT_DATA_SOURCE_BASE;
  log(`Syncing reference data from ${base}`);
  const [remoteExtruders, remoteHotends, remoteProbes, remoteToolheads] = await Promise.all([
    fetchJson(`${base}/extruders.json`),
    fetchJson(`${base}/hotends.json`),
    fetchJson(`${base}/probes.json`),
    fetchJson(`${base}/toolheads.json`)
  ]);
  return {
    extruders: hasNamedArray(remoteExtruders, "extruders") ? remoteExtruders.extruders : extruders_default.extruders,
    hotends: hasNamedArray(remoteHotends, "hotends") ? remoteHotends.hotends : hotends_default.hotends,
    probes: hasNamedArray(remoteProbes, "probes") ? remoteProbes.probes : probes_default.probes,
    toolheads: hasNamedArray(remoteToolheads, "toolheads") ? remoteToolheads.toolheads : toolheads_default.toolheads
  };
}
__name(loadReferenceData, "loadReferenceData");
function buildScanTargets(toolheads, extraLocations, blacklist, log) {
  const blacklistSet = new Set(blacklist.map((e) => e.toLowerCase()));
  const targets = toolheads.filter((toolhead) => !blacklistSet.has(toolhead.name.toLowerCase())).map((toolhead) => ({
    name: toolhead.name,
    url: String(toolhead.url ?? "").trim(),
    source: "toolheads.json"
  }));
  for (const entry of extraLocations) {
    if (!blacklistSet.has(entry.toolhead.toLowerCase())) {
      targets.push({
        name: entry.toolhead,
        url: entry.url,
        source: "extra_github_locations.json"
      });
    }
  }
  if (blacklist.length > 0) {
    log(`Blacklist: skipping ${blacklist.length} toolhead(s): ${blacklist.join(", ")}`);
  }
  if (extraLocations.length > 0) {
    log(`Loaded ${extraLocations.length} extra GitHub location(s)`);
  }
  return targets;
}
__name(buildScanTargets, "buildScanTargets");
function stripHtmlTags(html) {
  let text = html;
  text = text.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, " ");
  text = text.replace(/<style[^>]*>[\s\S]*?<\/style>/gi, " ");
  text = text.replace(/<nav[^>]*>[\s\S]*?<\/nav>/gi, " ");
  text = text.replace(/<footer[^>]*>[\s\S]*?<\/footer>/gi, " ");
  text = text.replace(/<br\s*\/?>/gi, "\n");
  text = text.replace(/<\/(?:p|div|li|tr|h[1-6]|td|th|dt|dd|section|article)>/gi, "\n");
  text = text.replace(/<[^>]+>/g, " ");
  text = text.replace(/&nbsp;/gi, " ");
  text = text.replace(/&amp;/gi, "&");
  text = text.replace(/&lt;/gi, "<");
  text = text.replace(/&gt;/gi, ">");
  text = text.replace(/&quot;/gi, '"');
  text = text.replace(/&#39;/gi, "'");
  text = text.replace(/[ \t]+/g, " ");
  text = text.replace(/\n{3,}/g, "\n\n");
  return text.trim();
}
__name(stripHtmlTags, "stripHtmlTags");
function isHtmlContent(text) {
  return /<!doctype\s+html|<html[\s>]/i.test(text.slice(0, 500));
}
__name(isHtmlContent, "isHtmlContent");
async function collectReadmeBlocks(env, scanTargets, skipUnchanged, log) {
  const readmeBlocks = {};
  const updatedHashes = [];
  let fetchBudget = SCAN_FETCH_BUDGET;
  for (const target of scanTargets) {
    const name = target.name || "Unknown";
    const url = target.url.trim();
    const isGitHub = url.includes("github.com");
    if (fetchBudget <= 0) {
      log(`  [SKIP]  ${name}  (fetch budget exhausted)`);
      continue;
    }
    let readmeText = null;
    if (isGitHub) {
      for (const candidate of githubContentCandidates(url, target.source)) {
        if (fetchBudget <= 0) break;
        fetchBudget--;
        readmeText = await fetchText(candidate);
        if (readmeText !== null) {
          break;
        }
      }
    } else if (url.startsWith("http://") || url.startsWith("https://")) {
      fetchBudget--;
      const rawResponse = await fetchText(url);
      if (rawResponse !== null) {
        readmeText = isHtmlContent(rawResponse) ? stripHtmlTags(rawResponse) : rawResponse;
      }
    }
    if (readmeText === null) {
      log(`  [FAIL]  ${name}  (no content found)`);
      continue;
    }
    const currentHash = await sha256Hex(readmeText);
    updatedHashes.push([url, currentHash]);
    if (skipUnchanged) {
      const cachedHash = await env.HASH_CACHE.get(hashKey(url));
      if (cachedHash === currentHash) {
        log(`  [SKIP]  ${name}  (content unchanged)`);
        continue;
      }
    }
    log(`  [OK]    ${name}${isGitHub ? "" : "  (HTML page)"}`);
    readmeBlocks[name] = readmeBlocks[name] ? `${readmeBlocks[name]}

${readmeText}` : readmeText;
  }
  if (fetchBudget <= 0) {
    log(`[WARN] Fetch budget (${SCAN_FETCH_BUDGET}) exhausted \u2014 some sources were skipped`);
  }
  return { readmeBlocks, updatedHashes };
}
__name(collectReadmeBlocks, "collectReadmeBlocks");
function escapeRegExp(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
__name(escapeRegExp, "escapeRegExp");
function buildSearchTable(canonicalNames, aliasMap) {
  const entries = /* @__PURE__ */ new Map();
  for (const name of canonicalNames) {
    entries.set(name.toLowerCase(), name);
  }
  for (const [alias, canonical] of Object.entries(aliasMap)) {
    entries.set(alias.toLowerCase(), canonical);
  }
  return [...entries.entries()].sort((left, right) => right[0].length - left[0].length);
}
__name(buildSearchTable, "buildSearchTable");
function buildBoardSearchTable(boardAliasMap) {
  return Object.entries(boardAliasMap).map(([alias, canonical]) => [alias.toLowerCase(), canonical]).sort((left, right) => right[0].length - left[0].length);
}
__name(buildBoardSearchTable, "buildBoardSearchTable");
function findMatches(text, searchTable) {
  const textLower = text.toLowerCase();
  const foundLower = /* @__PURE__ */ new Set();
  const foundNames = [];
  const foundSources = {};
  const matchedSpans = [];
  for (const [termLower, canonical] of searchTable) {
    if (termLower === "bmg") {
      continue;
    }
    const pattern = new RegExp(`\\b${escapeRegExp(termLower)}\\b`, "g");
    let match;
    while ((match = pattern.exec(textLower)) !== null) {
      const spanStart = match.index;
      const spanEnd = spanStart + match[0].length;
      const overlaps = matchedSpans.some(([start, end]) => spanStart < end && spanEnd > start);
      if (overlaps) {
        continue;
      }
      const key = canonical.toLowerCase();
      if (foundLower.has(key)) {
        break;
      }
      foundLower.add(key);
      foundNames.push(canonical);
      matchedSpans.push([spanStart, spanEnd]);
      const lineStart = text.lastIndexOf("\n", spanStart - 1) + 1;
      const rawLineEnd = text.indexOf("\n", spanEnd);
      const lineEnd = rawLineEnd === -1 ? text.length : rawLineEnd;
      foundSources[canonical] = text.slice(lineStart, lineEnd).trim();
      break;
    }
  }
  return { foundNames, foundSources };
}
__name(findMatches, "findMatches");
function isPartCoolingContext(localText) {
  return PART_COOLING_CONTEXT_PATTERNS.some((pattern) => new RegExp(pattern, "i").test(localText));
}
__name(isPartCoolingContext, "isPartCoolingContext");
function isHotendContext(localText) {
  return HOTEND_CONTEXT_PATTERNS.some((pattern) => new RegExp(pattern, "i").test(localText));
}
__name(isHotendContext, "isHotendContext");
function findFans(text) {
  const hotendFans = [];
  const partCoolingFans = [];
  const hotendSources = {};
  const partCoolingSources = {};
  for (const line of text.split(/\r?\n/)) {
    const lineLower = line.toLowerCase();
    const contextIsFan = ["fan", "blower", "cpap", "axial"].some((word) => lineLower.includes(word));
    for (const [canonical, patterns] of FAN_PATTERNS) {
      let matched = false;
      for (const pattern of patterns) {
        const regex = new RegExp(pattern, "ig");
        let match;
        while ((match = regex.exec(line)) !== null) {
          if (canonical !== "CPAP" && !contextIsFan) {
            continue;
          }
          const start = Math.max(0, match.index - 48);
          const end = Math.min(line.length, match.index + match[0].length + 48);
          const localContext = line.slice(start, end);
          const isPart = isPartCoolingContext(localContext);
          const isHotend = isHotendContext(localContext);
          if (isPart && !partCoolingFans.includes(canonical)) {
            partCoolingFans.push(canonical);
            partCoolingSources[canonical] = line.trim();
          }
          if (isHotend && !hotendFans.includes(canonical)) {
            hotendFans.push(canonical);
            hotendSources[canonical] = line.trim();
          }
          if (canonical === "CPAP" && !isPart && !isHotend && !partCoolingFans.includes(canonical)) {
            partCoolingFans.push(canonical);
            partCoolingSources[canonical] = line.trim();
          }
          matched = true;
          break;
        }
        if (matched) {
          break;
        }
      }
    }
  }
  return { hotendFans, partCoolingFans, hotendSources, partCoolingSources };
}
__name(findFans, "findFans");
function findFilamentCutterSource(text) {
  for (const line of text.split(/\r?\n/)) {
    for (const pattern of FILAMENT_CUTTER_PATTERNS) {
      if (new RegExp(pattern, "i").test(line)) {
        return line.trim();
      }
    }
  }
  return null;
}
__name(findFilamentCutterSource, "findFilamentCutterSource");
function findPrinters(text, printerAliasMap) {
  const found = /* @__PURE__ */ new Set();
  const printers = [];
  const printerSources = {};
  for (const [canonical, patterns] of PRINTER_PATTERNS) {
    if (found.has(canonical)) continue;
    for (const pattern of patterns) {
      const regex = new RegExp(pattern, "ig");
      const match = regex.exec(text);
      if (match) {
        found.add(canonical);
        printers.push(canonical);
        const lineStart = text.lastIndexOf("\n", match.index - 1) + 1;
        const rawLineEnd = text.indexOf("\n", match.index + match[0].length);
        const lineEnd = rawLineEnd === -1 ? text.length : rawLineEnd;
        printerSources[canonical] = text.slice(lineStart, lineEnd).trim();
        break;
      }
    }
  }
  for (const [alias, canonical] of Object.entries(printerAliasMap)) {
    if (found.has(canonical)) continue;
    const aliasPattern = new RegExp(`\\b${escapeRegExp(alias)}\\b`, "ig");
    const match = aliasPattern.exec(text);
    if (match) {
      found.add(canonical);
      printers.push(canonical);
      const lineStart = text.lastIndexOf("\n", match.index - 1) + 1;
      const rawLineEnd = text.indexOf("\n", match.index + match[0].length);
      const lineEnd = rawLineEnd === -1 ? text.length : rawLineEnd;
      printerSources[canonical] = text.slice(lineStart, lineEnd).trim();
    }
  }
  return { printers, printerSources };
}
__name(findPrinters, "findPrinters");
function findDirectBeltPaths(text) {
  const found = /* @__PURE__ */ new Set();
  const beltPaths = [];
  const beltPathSources = {};
  for (const [canonical, patterns] of BELT_PATH_PATTERNS) {
    if (found.has(canonical)) continue;
    for (const pattern of patterns) {
      const regex = new RegExp(pattern, "ig");
      const match = regex.exec(text);
      if (match) {
        found.add(canonical);
        beltPaths.push(canonical);
        const lineStart = text.lastIndexOf("\n", match.index - 1) + 1;
        const rawLineEnd = text.indexOf("\n", match.index + match[0].length);
        const lineEnd = rawLineEnd === -1 ? text.length : rawLineEnd;
        beltPathSources[canonical] = text.slice(lineStart, lineEnd).trim();
        break;
      }
    }
  }
  return { beltPaths, beltPathSources };
}
__name(findDirectBeltPaths, "findDirectBeltPaths");
function deriveBeltPaths(printers) {
  const pathSet = /* @__PURE__ */ new Set();
  for (const printer of printers) {
    const belt = PRINTER_TO_BELT_PATH[printer];
    if (belt && belt !== "unknown") {
      pathSet.add(belt);
    }
  }
  return [...pathSet];
}
__name(deriveBeltPaths, "deriveBeltPaths");
function normalizeField(value) {
  if (value === null || value === void 0) {
    return /* @__PURE__ */ new Set();
  }
  if (Array.isArray(value)) {
    return new Set(
      value.filter((entry) => typeof entry === "string").map((entry) => entry.toLowerCase().trim()).filter((entry) => !PLACEHOLDER_VALUES.has(entry))
    );
  }
  if (typeof value === "string") {
    const normalized = value.toLowerCase().trim();
    return PLACEHOLDER_VALUES.has(normalized) ? /* @__PURE__ */ new Set() : /* @__PURE__ */ new Set([normalized]);
  }
  return /* @__PURE__ */ new Set();
}
__name(normalizeField, "normalizeField");
function canonicalLookup(values, searchTable) {
  const reverseMap = new Map(searchTable.map(([term, canonical]) => [term, canonical.toLowerCase()]));
  return new Set([...values].map((value) => reverseMap.get(value) ?? value));
}
__name(canonicalLookup, "canonicalLookup");
function findNewItems(foundCanonicals, existingFieldValue, searchTable) {
  const existingNormalized = normalizeField(existingFieldValue);
  const existingCanonical = canonicalLookup(existingNormalized, searchTable);
  return foundCanonicals.filter((item) => !existingCanonical.has(item.toLowerCase()));
}
__name(findNewItems, "findNewItems");
function findNewSimple(foundItems, existingValue) {
  const existingNormalized = normalizeField(existingValue);
  return foundItems.filter((item) => !existingNormalized.has(item.toLowerCase()));
}
__name(findNewSimple, "findNewSimple");
function mergeField(existing, newItems) {
  if (newItems.length === 0) {
    return existing;
  }
  if (existing === null || existing === void 0) {
    return newItems.length === 1 ? newItems[0] : newItems;
  }
  if (typeof existing === "string" && PLACEHOLDER_VALUES.has(existing.toLowerCase().trim())) {
    return newItems.length === 1 ? newItems[0] : newItems;
  }
  const existingList = Array.isArray(existing) ? existing : [existing];
  return [...existingList, ...newItems];
}
__name(mergeField, "mergeField");
function getExistingFanField(toolhead, pluralKey, singularKey) {
  if (pluralKey in toolhead) {
    return { value: toolhead[pluralKey], key: pluralKey };
  }
  return { value: toolhead[singularKey], key: singularKey };
}
__name(getExistingFanField, "getExistingFanField");
function hashKey(url) {
  return `hash:${url}`;
}
__name(hashKey, "hashKey");
async function sha256Hex(content) {
  const bytes = new TextEncoder().encode(content);
  const digest = await crypto.subtle.digest("SHA-256", bytes);
  return [...new Uint8Array(digest)].map((value) => value.toString(16).padStart(2, "0")).join("");
}
__name(sha256Hex, "sha256Hex");
function stableClone(value) {
  return JSON.parse(JSON.stringify(value));
}
__name(stableClone, "stableClone");
function buildEmailBodies(results) {
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const lines = [
    `Toolhead Scanner - changes detected at ${now}`,
    "========================================================",
    `${results.length} toolhead(s) with updates:`,
    ""
  ];
  for (const entry of results) {
    const name = entry.name;
    lines.push(`  ${name}`);
    lines.push(`  ${"-".repeat(name.length)}`);
    const sections = [
      ["Extruders", entry.new_extruders],
      ["Hotends", entry.new_hotends],
      ["Probes", entry.new_probes],
      ["Boards", entry.new_boards],
      ["Hotend Fans", entry.new_hotend_fans],
      ["Part Cooling Fans", entry.new_part_cooling_fans],
      ["Printers", entry.new_printers],
      ["Belt Paths", entry.new_belt_paths]
    ];
    for (const [label, items] of sections) {
      if (items.length > 0) {
        lines.push(`    NEW ${label}: ${items.join(", ")}`);
      }
    }
    if (entry.new_filament_cutter) {
      lines.push("    NEW Filament Cutter: supported");
    }
    lines.push("");
  }
  const htmlParts = [
    "<html><body style='font-family:Consolas,monospace;font-size:14px;'>",
    "<h2>Toolhead Scanner &mdash; changes detected</h2>",
    `<p style='color:#888;'>${escapeHtml(now)}</p>`,
    `<p><strong>${results.length}</strong> toolhead(s) with updates:</p>`
  ];
  for (const entry of results) {
    htmlParts.push(`<h3 style='color:#61afef;'>${escapeHtml(entry.name)}</h3>`);
    htmlParts.push("<table style='border-collapse:collapse;margin-left:16px;'>");
    const sections = [
      ["Extruders", entry.new_extruders],
      ["Hotends", entry.new_hotends],
      ["Probes", entry.new_probes],
      ["Boards", entry.new_boards],
      ["Hotend Fans", entry.new_hotend_fans],
      ["Part Cooling Fans", entry.new_part_cooling_fans],
      ["Printers", entry.new_printers],
      ["Belt Paths", entry.new_belt_paths]
    ];
    for (const [label, items] of sections) {
      if (items.length === 0) {
        continue;
      }
      htmlParts.push(
        `<tr><td style='padding:2px 12px 2px 0;color:#c678dd;'>${escapeHtml(label)}</td><td style='color:#98c379;'>${escapeHtml(items.join(", "))}</td></tr>`
      );
    }
    if (entry.new_filament_cutter) {
      htmlParts.push("<tr><td style='padding:2px 12px 2px 0;color:#c678dd;'>Filament Cutter</td><td style='color:#98c379;'>supported</td></tr>");
    }
    htmlParts.push("</table>");
  }
  htmlParts.push("</body></html>");
  return { text: lines.join("\n"), html: htmlParts.join("\n") };
}
__name(buildEmailBodies, "buildEmailBodies");
function escapeHtml(value) {
  return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
}
__name(escapeHtml, "escapeHtml");
async function runScan(env, options) {
  const startedAt = (/* @__PURE__ */ new Date()).toISOString();
  const logs = [];
  const recheck = options.recheck ?? false;
  const log = /* @__PURE__ */ __name((message) => {
    logs.push(message);
    options.log?.(message);
  }, "log");
  try {
    if (recheck) {
      log("[INFO] Rechecking all READMEs");
    }
    const referenceData = await loadReferenceData(log, env.TOOLHEAD_DATA_SOURCE_BASE, true);
    const aliases = sanitizeAliases(alias_default);
    const extraLocations = await loadExtraLocations(env);
    const blacklist = await loadBlacklist(env);
    const scanTargets = buildScanTargets(referenceData.toolheads, extraLocations, blacklist, log);
    const toolheadMap = new Map(referenceData.toolheads.map((toolhead) => [toolhead.name, toolhead]));
    log(`Downloading READMEs for ${scanTargets.length} source location(s) ...`);
    const { readmeBlocks, updatedHashes } = await collectReadmeBlocks(env, scanTargets, !recheck, log);
    log("");
    const extruderTable = buildSearchTable(referenceData.extruders.map((entry) => entry.name), aliases.extruders);
    const hotendTable = buildSearchTable(referenceData.hotends.map((entry) => entry.name), aliases.hotends);
    const probeTable = buildSearchTable(referenceData.probes.map((entry) => entry.name), aliases.probes);
    const boardTable = buildBoardSearchTable(aliases.boards);
    const results = [];
    const updatesNeeded = [];
    for (const [name, readmeText] of Object.entries(readmeBlocks)) {
      const toolhead = toolheadMap.get(name);
      if (!toolhead) {
        continue;
      }
      const { foundNames: foundExtruders, foundSources: extruderSources } = findMatches(readmeText, extruderTable);
      const { foundNames: foundHotends, foundSources: hotendMatchSources } = findMatches(readmeText, hotendTable);
      const { foundNames: foundProbes, foundSources: probeSources } = findMatches(readmeText, probeTable);
      const { foundNames: foundBoards, foundSources: boardSources } = findMatches(readmeText, boardTable);
      const { hotendFans, partCoolingFans, hotendSources: hotendFanSources, partCoolingSources } = findFans(readmeText);
      const filamentCutterSource = findFilamentCutterSource(readmeText);
      const filteredExtruders = foundExtruders.filter((entry) => entry.toLowerCase() !== "bmg");
      const filteredBoards = foundBoards.filter((entry) => entry.toLowerCase() !== "bmg");
      const trimmedExtruderSources = Object.fromEntries(
        Object.entries(extruderSources).filter(([key]) => key.toLowerCase() !== "bmg")
      );
      const trimmedBoardSources = Object.fromEntries(
        Object.entries(boardSources).filter(([key]) => key.toLowerCase() !== "bmg")
      );
      const normalizedExtruders = filteredExtruders.map((entry) => entry === "HextrudORT" ? "Vz HextrudORT" : entry);
      if (trimmedExtruderSources.HextrudORT) {
        trimmedExtruderSources["Vz HextrudORT"] = trimmedExtruderSources.HextrudORT;
        delete trimmedExtruderSources.HextrudORT;
      }
      const dedupedExtruders = normalizedExtruders.filter((entry, index) => normalizedExtruders.indexOf(entry) === index);
      const newExtruders = findNewItems(dedupedExtruders, toolhead.extruders, extruderTable);
      const newHotends = findNewItems(foundHotends, toolhead.hotend, hotendTable);
      const newProbes = findNewItems(foundProbes, toolhead.probe, probeTable);
      const newBoards = findNewItems(filteredBoards, toolhead.boards, boardTable);
      const existingHotendFan = getExistingFanField(toolhead, "hotend_fans", "hotend_fan");
      const existingPartCoolingFan = getExistingFanField(toolhead, "part_cooling_fans", "part_cooling_fan");
      const newHotendFans = findNewSimple(hotendFans, existingHotendFan.value);
      const newPartCoolingFans = findNewSimple(partCoolingFans, existingPartCoolingFan.value);
      const existingCutter = String(toolhead.filament_cutter ?? "").toLowerCase().trim();
      const newFilamentCutter = Boolean(filamentCutterSource && PLACEHOLDER_VALUES.has(existingCutter));
      const { printers: foundPrinters, printerSources } = findPrinters(readmeText, aliases.printers);
      const newPrinters = findNewSimple(foundPrinters, toolhead.printer_compatibility);
      const allPrinters = [.../* @__PURE__ */ new Set([
        ...Array.isArray(toolhead.printer_compatibility) ? toolhead.printer_compatibility : toolhead.printer_compatibility && !PLACEHOLDER_VALUES.has(String(toolhead.printer_compatibility).toLowerCase().trim()) ? [String(toolhead.printer_compatibility)] : [],
        ...newPrinters
      ])];
      const derivedBeltPaths = deriveBeltPaths(allPrinters);
      const { beltPaths: directBeltPaths, beltPathSources: directBeltSources } = findDirectBeltPaths(readmeText);
      const allFoundBeltPaths = [.../* @__PURE__ */ new Set([...derivedBeltPaths, ...directBeltPaths])];
      const newBeltPaths = findNewSimple(allFoundBeltPaths, toolhead.belt_path);
      if (!newExtruders.length && !newHotends.length && !newProbes.length && !newBoards.length && !newHotendFans.length && !newPartCoolingFans.length && !newFilamentCutter && !newPrinters.length && !newBeltPaths.length) {
        continue;
      }
      const sources = {};
      for (const item of newExtruders) {
        sources[item] = trimmedExtruderSources[item] ?? "Unknown source";
      }
      for (const item of newHotends) {
        sources[item] = hotendMatchSources[item] ?? "Unknown source";
      }
      for (const item of newProbes) {
        sources[item] = probeSources[item] ?? "Unknown source";
      }
      for (const item of newBoards) {
        sources[item] = trimmedBoardSources[item] ?? "Unknown source";
      }
      for (const item of newHotendFans) {
        sources[`hotend_fan::${item}`] = hotendFanSources[item] ?? "Unknown source";
      }
      for (const item of newPartCoolingFans) {
        sources[`part_cooling_fan::${item}`] = partCoolingSources[item] ?? "Unknown source";
      }
      if (newFilamentCutter) {
        sources.filament_cutter = filamentCutterSource ?? "Unknown source";
      }
      for (const item of newPrinters) {
        sources[`printer::${item}`] = printerSources[item] ?? "Unknown source";
      }
      for (const item of newBeltPaths) {
        sources[`belt_path::${item}`] = directBeltSources[item] ?? "Derived from printer compatibility";
      }
      log(`Toolhead : ${name}`);
      const logSections = [
        ["Extruders", newExtruders],
        ["Hotends", newHotends],
        ["Probes", newProbes],
        ["Boards", newBoards],
        ["Hotend Fans", newHotendFans],
        ["Part Cooling Fans", newPartCoolingFans],
        ["Printers", newPrinters],
        ["Belt Paths", newBeltPaths]
      ];
      for (const [label, items] of logSections) {
        if (!items.length) {
          continue;
        }
        log(`  NEW ${label}: ${items.join(", ")}`);
      }
      if (newFilamentCutter) {
        log("  NEW Filament Cutter: supported");
      }
      const updated = stableClone(toolhead);
      if (newExtruders.length) {
        updated.extruders = mergeField(toolhead.extruders, newExtruders);
      }
      if (newHotends.length) {
        updated.hotend = mergeField(toolhead.hotend, newHotends);
      }
      if (newProbes.length) {
        updated.probe = mergeField(toolhead.probe, newProbes);
      }
      if (newBoards.length) {
        updated.boards = mergeField(toolhead.boards, newBoards);
      }
      if (newHotendFans.length) {
        updated[existingHotendFan.key] = mergeField(existingHotendFan.value, newHotendFans);
      }
      if (newPartCoolingFans.length) {
        updated[existingPartCoolingFan.key] = mergeField(existingPartCoolingFan.value, newPartCoolingFans);
      }
      if (newFilamentCutter) {
        updated.filament_cutter = "supported";
      }
      if (newPrinters.length) {
        updated.printer_compatibility = mergeField(toolhead.printer_compatibility, newPrinters);
      }
      if (newBeltPaths.length) {
        updated.belt_path = mergeField(toolhead.belt_path, newBeltPaths);
      }
      updated._new_item_sources = sources;
      updatesNeeded.push(updated);
      results.push({
        name,
        new_extruders: newExtruders,
        new_hotends: newHotends,
        new_probes: newProbes,
        new_boards: newBoards,
        new_hotend_fans: newHotendFans,
        new_part_cooling_fans: newPartCoolingFans,
        new_filament_cutter: newFilamentCutter,
        new_printers: newPrinters,
        new_belt_paths: newBeltPaths,
        sources,
        updated,
        original: stableClone(toolhead)
      });
    }
    await Promise.all(updatedHashes.map(async ([url, hash]) => env.HASH_CACHE.put(hashKey(url), hash)));
    const finishedAt = (/* @__PURE__ */ new Date()).toISOString();
    const changed = updatesNeeded.length > 0;
    const summary = changed ? `${updatesNeeded.length} toolhead(s) changed` : "No changes detected";
    if (changed) {
      log(`Updated entries prepared for ${updatesNeeded.length} toolhead(s)`);
    } else {
      log("No Updates Needed");
    }
    return {
      ok: true,
      trigger: options.trigger,
      recheck,
      startedAt,
      finishedAt,
      changed,
      changeCount: updatesNeeded.length,
      summary,
      results,
      updatedPayload: { toolheads: updatesNeeded },
      logs
    };
  } catch (error) {
    const finishedAt = (/* @__PURE__ */ new Date()).toISOString();
    return {
      ok: false,
      trigger: options.trigger,
      recheck,
      startedAt,
      finishedAt,
      changed: false,
      changeCount: 0,
      summary: "Scan failed",
      results: [],
      updatedPayload: { toolheads: [] },
      logs,
      error: error instanceof Error ? error.message : String(error)
    };
  }
}
__name(runScan, "runScan");
async function loadEditorData(dataSourceBase) {
  const ref = await loadReferenceData(() => {
  }, dataSourceBase);
  const aliases = sanitizeAliases(alias_default);
  const boardNames = [...new Set(Object.values(aliases.boards))].sort();
  return {
    toolheads: ref.toolheads,
    extruderNames: ref.extruders.map((e) => e.name).sort(),
    hotendNames: ref.hotends.map((e) => e.name).sort(),
    probeNames: ref.probes.map((e) => e.name).sort(),
    extruders: ref.extruders,
    hotends: ref.hotends,
    probes: ref.probes,
    boards: boardNames,
    fans: FAN_PATTERNS.map(([name]) => name),
    filamentCutterOptions: ["native", "mod", "unknown", "unsupported"],
    categoryOptions: ["Printers for Ants", "Full Size Printers"],
    printerOptions: PRINTER_PATTERNS.map(([name]) => name).sort(),
    beltPathOptions: [.../* @__PURE__ */ new Set([...Object.values(PRINTER_TO_BELT_PATH).filter((v) => v !== "unknown"), ...BELT_PATH_PATTERNS.map(([name]) => name)])].sort()
  };
}
__name(loadEditorData, "loadEditorData");
async function scanSingleToolhead(url) {
  const ref = await loadReferenceData(() => {
  }, void 0, true);
  const aliases = sanitizeAliases(alias_default);
  const trimmedUrl = url.trim();
  const isGitHub = trimmedUrl.includes("github.com");
  let readmeText = null;
  if (isGitHub) {
    const dummyTarget = { name: "", url: trimmedUrl, source: "toolheads.json" };
    for (const candidate of githubContentCandidates(trimmedUrl, dummyTarget.source)) {
      readmeText = await fetchText(candidate);
      if (readmeText !== null) break;
    }
  } else if (trimmedUrl.startsWith("http://") || trimmedUrl.startsWith("https://")) {
    const rawResponse = await fetchText(trimmedUrl);
    if (rawResponse !== null) {
      readmeText = isHtmlContent(rawResponse) ? stripHtmlTags(rawResponse) : rawResponse;
    }
  }
  if (readmeText === null) {
    return {
      extruders: [],
      hotends: [],
      probes: [],
      boards: [],
      hotend_fans: [],
      part_cooling_fans: [],
      filament_cutter: null,
      printer_compatibility: [],
      belt_path: [],
      sources: {}
    };
  }
  const extruderTable = buildSearchTable(ref.extruders.map((e) => e.name), aliases.extruders);
  const hotendTable = buildSearchTable(ref.hotends.map((e) => e.name), aliases.hotends);
  const probeTable = buildSearchTable(ref.probes.map((e) => e.name), aliases.probes);
  const boardTable = buildBoardSearchTable(aliases.boards);
  const { foundNames: foundExtruders, foundSources: extruderSources } = findMatches(readmeText, extruderTable);
  const { foundNames: foundHotends, foundSources: hotendSources } = findMatches(readmeText, hotendTable);
  const { foundNames: foundProbes, foundSources: probeSources } = findMatches(readmeText, probeTable);
  const { foundNames: foundBoards, foundSources: boardSources } = findMatches(readmeText, boardTable);
  const { hotendFans, partCoolingFans, hotendSources: hotendFanSources, partCoolingSources } = findFans(readmeText);
  const filamentCutterSource = findFilamentCutterSource(readmeText);
  const filteredExtruders = foundExtruders.filter((e) => e.toLowerCase() !== "bmg");
  const filteredBoards = foundBoards.filter((e) => e.toLowerCase() !== "bmg");
  const normalizedExtruders = filteredExtruders.map((e) => e === "HextrudORT" ? "Vz HextrudORT" : e);
  const dedupedExtruders = normalizedExtruders.filter((e, i) => normalizedExtruders.indexOf(e) === i);
  const { printers, printerSources } = findPrinters(readmeText, aliases.printers);
  const derivedBeltPaths = deriveBeltPaths(printers);
  const { beltPaths: directBeltPaths, beltPathSources: directBeltSources } = findDirectBeltPaths(readmeText);
  const allBeltPaths = [.../* @__PURE__ */ new Set([...derivedBeltPaths, ...directBeltPaths])];
  const sources = {};
  for (const item of dedupedExtruders) sources[item] = extruderSources[item === "Vz HextrudORT" ? "HextrudORT" : item] ?? extruderSources[item] ?? "";
  for (const item of foundHotends) sources[item] = hotendSources[item] ?? "";
  for (const item of foundProbes) sources[item] = probeSources[item] ?? "";
  for (const item of filteredBoards) sources[item] = boardSources[item] ?? "";
  for (const item of hotendFans) sources[`hotend_fan::${item}`] = hotendFanSources[item] ?? "";
  for (const item of partCoolingFans) sources[`part_cooling_fan::${item}`] = partCoolingSources[item] ?? "";
  if (filamentCutterSource) sources.filament_cutter = filamentCutterSource;
  for (const item of printers) sources[`printer::${item}`] = printerSources[item] ?? "";
  for (const item of allBeltPaths) sources[`belt_path::${item}`] = directBeltSources[item] ?? "Derived from printer compatibility";
  return {
    extruders: dedupedExtruders,
    hotends: foundHotends,
    probes: foundProbes,
    boards: filteredBoards,
    hotend_fans: hotendFans,
    part_cooling_fans: partCoolingFans,
    filament_cutter: filamentCutterSource ? "supported" : null,
    printer_compatibility: printers,
    belt_path: allBeltPaths,
    sources
  };
}
__name(scanSingleToolhead, "scanSingleToolhead");

// src/index.ts
var LAST_RUN_KEY = "last-run";
function jsonResponse(data, status = 200) {
  return new Response(JSON.stringify(data, null, 2), {
    status,
    headers: {
      "content-type": "application/json; charset=utf-8"
    }
  });
}
__name(jsonResponse, "jsonResponse");
function textResponse(body, status = 200) {
  return new Response(body, {
    status,
    headers: {
      "content-type": "text/plain; charset=utf-8"
    }
  });
}
__name(textResponse, "textResponse");
function htmlResponse(body, status = 200) {
  return new Response(body, {
    status,
    headers: {
      "content-type": "text/html; charset=utf-8",
      "cache-control": "no-store"
    }
  });
}
__name(htmlResponse, "htmlResponse");
function dashboardHtml() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ToolheadScanner Dashboard</title>
  <style>
    :root {
      --bg: #0b0f16;
      --panel: #141b26;
      --ink: #e8ecf4;
      --muted: #a1adbf;
      --line: #2a3547;
      --brand: #3c82f6;
      --brand-2: #0ea5a0;
      --ok: #3ccf8e;
      --error: #ff7474;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: radial-gradient(circle at 15% -10%, #1d283b 0, var(--bg) 44%, #070a10 100%);
      color: var(--ink);
    }
    .wrap { max-width: 1120px; margin: 0 auto; padding: 24px; }
    .hero {
      background: linear-gradient(120deg, #182131, #111824);
      border: 1px solid var(--line);
      border-radius: 14px;
      padding: 20px;
      box-shadow: 0 10px 28px rgba(0, 0, 0, 0.35);
      margin-bottom: 16px;
    }
    h1 { margin: 0 0 8px; font-size: 28px; letter-spacing: 0.2px; }
    .muted { color: var(--muted); }
    .grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 14px;
    }
    @media (min-width: 960px) {
      .grid { grid-template-columns: 1.1fr 0.9fr; }
    }
    .grid > * { min-width: 0; }
    .card {
      border: 1px solid var(--line);
      border-radius: 12px;
      background: var(--panel);
      padding: 14px;
      min-width: 0;
    }
    .row { display: flex; gap: 8px; flex-wrap: wrap; align-items: center; }
    label { font-size: 13px; color: var(--muted); display: block; margin-bottom: 4px; }
    input, textarea {
      width: 100%;
      border: 1px solid #32425c;
      border-radius: 8px;
      padding: 10px;
      font: inherit;
      background: #0d1420;
      color: var(--ink);
    }
    textarea { min-height: 120px; }
    button {
      border: 1px solid #2f6ad0;
      background: var(--brand);
      color: white;
      border-radius: 8px;
      padding: 9px 14px;
      font-weight: 600;
      cursor: pointer;
    }
    button.alt {
      border-color: #0d8f8b;
      background: var(--brand-2);
    }
    button.ghost {
      background: transparent;
      color: var(--ink);
      border-color: #4c5f7e;
    }
    button:disabled { opacity: 0.5; cursor: default; }
    .status {
      margin-top: 8px;
      padding: 8px 10px;
      border-radius: 8px;
      border: 1px solid var(--line);
      background: #121a27;
      font-size: 13px;
    }
    .status.ok { border-color: #3f8b68; color: var(--ok); background: #11241b; }
    .status.error { border-color: #8b3f3f; color: var(--error); background: #261315; }
    .list { margin-top: 8px; border-top: 1px dashed var(--line); }
    .item {
      display: grid;
      gap: 6px;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      border-bottom: 1px dashed var(--line);
      padding: 9px 0;
    }
    .item-main {
      min-width: 0;
    }
    .item-link {
      display: block;
      min-width: 0;
      max-width: 100%;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      color: #8cb3ff;
      text-decoration: none;
    }
    .item-link:hover {
      text-decoration: underline;
      color: #b8d0ff;
    }
    .small { font-size: 12px; color: var(--muted); }
    pre {
      margin: 0;
      padding: 10px;
      border-radius: 8px;
      background: #080c13;
      color: #e2e8f0;
      overflow-x: auto;
      overflow-y: auto;
      width: 100%;
      max-width: 100%;
      height: 420px;
      white-space: pre;
      font-size: 12px;
      border: 1px solid #27344a;
    }
    /* Tabs */
    .tabs { display: flex; gap: 0; margin-bottom: 16px; }
    .tab-btn {
      padding: 10px 20px;
      border: 1px solid var(--line);
      border-bottom: none;
      background: transparent;
      color: var(--muted);
      cursor: pointer;
      border-radius: 8px 8px 0 0;
      font-weight: 600;
      font-size: 14px;
    }
    .tab-btn.active { background: var(--panel); color: var(--ink); border-color: var(--brand); }
    .tab-content { display: none; }
    .tab-content.active { display: block; }
    /* Editor */
    .editor-toolbar { display: flex; gap: 8px; flex-wrap: wrap; align-items: center; margin-bottom: 16px; }
    .editor-toolbar select { flex: 1; min-width: 180px; }
    select {
      border: 1px solid #32425c;
      border-radius: 8px;
      padding: 10px;
      font: inherit;
      background: #0d1420;
      color: var(--ink);
      appearance: auto;
    }
    .field-grid { display: grid; gap: 10px; }
    .field-row {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 10px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: #0e1520;
    }
    .field-label {
      min-width: 130px;
      font-weight: 600;
      font-size: 13px;
      color: var(--brand);
      padding-top: 2px;
    }
    .field-value { flex: 1; min-width: 0; }
    .field-actions { display: flex; gap: 6px; flex-shrink: 0; }
    .chips { display: flex; flex-wrap: wrap; gap: 4px; }
    .chip {
      display: inline-block;
      padding: 3px 10px;
      border-radius: 12px;
      background: #1c2a3f;
      border: 1px solid #2c3e5a;
      font-size: 12px;
      color: var(--ink);
    }
    .chip.unknown { opacity: 0.5; font-style: italic; }
    .text-val { font-size: 13px; color: var(--ink); word-break: break-all; }
    .text-val.empty { color: var(--muted); font-style: italic; }
    /* Modal */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.7);
      z-index: 100;
      align-items: center;
      justify-content: center;
    }
    .modal-overlay.open { display: flex; }
    .modal {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 14px;
      padding: 20px;
      width: 90%;
      max-width: 520px;
      max-height: 80vh;
      overflow-y: auto;
      box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }
    .modal h3 { margin: 0 0 12px; font-size: 16px; }
    .modal-items { margin-bottom: 12px; }
    .modal-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 6px 0;
      border-bottom: 1px dashed var(--line);
    }
    .modal-item span { font-size: 13px; }
    .modal-add { display: flex; gap: 8px; margin-bottom: 12px; }
    .modal-add select { flex: 1; }
    .btn-sm { padding: 4px 10px; font-size: 12px; border-radius: 6px; }
    .btn-danger { background: #8b3f3f; border-color: #a04848; color: white; }
    .btn-danger:hover { background: #a04848; }
  </style>
</head>
<body>
  <div class="wrap">
    <section class="hero">
      <h1>ToolheadScanner Dashboard</h1>
      <p class="muted">Run scans, inspect last cron/manual result, manage extra GitHub locations, and configure the toolhead blacklist.</p>
      <div>
        <label for="token">Manual Run Token (Bearer)</label>
        <div class="row">
          <input id="token" type="password" placeholder="Paste MANUAL_RUN_TOKEN" />
          <button class="ghost" id="saveToken">Save</button>
          <button class="ghost" id="clearToken">Clear</button>
        </div>
      </div>
      <div id="status" class="status">Ready.</div>
    </section>

    <div class="tabs">
      <button class="tab-btn active" data-tab="scanner">Scanner</button>
      <button class="tab-btn" data-tab="editor">Data Editor</button>
    </div>

    <div id="tab-scanner" class="tab-content active">
    <div class="grid">
      <section class="card">
        <h2>Scan Controls</h2>
        <div class="row" style="margin-bottom: 10px;">
          <button id="runScan">Run Scan</button>
          <button class="alt" id="runRecheck">Run Full Recheck</button>
          <button class="ghost" id="loadLast">Refresh Last Run</button>
        </div>
        <p class="small">Run Scan respects hash cache. Run Full Recheck ignores hash cache.</p>
        <h3>Last Run Output</h3>
        <pre id="lastRun">No data loaded yet.</pre>
      </section>

      <section class="card">
        <h2>Extra GitHub Locations</h2>
        <div>
          <label for="toolhead">Toolhead</label>
          <input id="toolhead" type="text" placeholder="Hypernova" />
        </div>
        <div style="margin-top: 8px;">
          <label for="url">GitHub URL</label>
          <input id="url" type="url" placeholder="https://github.com/org/repo/blob/main/path/readme.md" />
        </div>
        <div class="row" style="margin-top: 10px;">
          <button id="addLocation">Add Location</button>
          <button class="ghost" id="refreshLocations">Refresh List</button>
        </div>
        <div id="locations" class="list"></div>
      </section>

      <section class="card">
        <h2>Toolhead Blacklist</h2>
        <p class="small" style="margin-top:0;">Blacklisted toolheads will be skipped during scans.</p>
        <div>
          <label for="blacklistToolhead">Toolhead Name</label>
          <input id="blacklistToolhead" type="text" placeholder="ToolheadName" />
        </div>
        <div class="row" style="margin-top: 10px;">
          <button id="addBlacklist">Add to Blacklist</button>
          <button class="ghost" id="refreshBlacklist">Refresh List</button>
        </div>
        <div id="blacklistItems" class="list"></div>
      </section>
    </div>
    </div>

    <div id="tab-editor" class="tab-content">
      <section class="card">
        <h2>Data Editor</h2>
        <div class="editor-toolbar">
          <select id="editorCategory">
            <option value="toolheads">Toolheads</option>
            <option value="extruders">Extruders</option>
            <option value="hotends">Hotends</option>
            <option value="probes">Probes</option>
          </select>
          <select id="editorSelect"><option value="">-- Load data first --</option></select>
          <button id="editorNewBtn">+ New</button>
          <button id="editorFieldsBtn" class="ghost">Manage Fields</button>
          <button id="editorLoadBtn" class="alt">Load Data</button>
          <button id="editorScanBtn" class="ghost" disabled>Scan Toolhead</button>
          <button id="editorPRBtn" class="ghost" disabled>Create Pull Request</button>
        </div>
        <div id="editorDetail"><p class="small">Click "Load Data" to fetch the current reference data.</p></div>
      </section>
    </div>
  </div>

  <div class="modal-overlay" id="modalOverlay">
    <div class="modal" id="modalContent"></div>
  </div>

  <script>
    const statusEl = document.getElementById("status");
    const tokenEl = document.getElementById("token");
    const lastRunEl = document.getElementById("lastRun");
    const locationsEl = document.getElementById("locations");

    const toolheadEl = document.getElementById("toolhead");
    const urlEl = document.getElementById("url");

    function loadSavedToken() {
      try {
        return localStorage.getItem("toolheadscanner_token") || "";
      } catch {
        return "";
      }
    }

    function saveTokenValue(value) {
      try {
        localStorage.setItem("toolheadscanner_token", value);
        return true;
      } catch {
        return false;
      }
    }

    function clearTokenValue() {
      try {
        localStorage.removeItem("toolheadscanner_token");
        return true;
      } catch {
        return false;
      }
    }

    tokenEl.value = loadSavedToken();

    function setStatus(message, type) {
      statusEl.textContent = message;
      statusEl.classList.remove("ok", "error");
      if (type) statusEl.classList.add(type);
    }

    function authHeaders(json = true) {
      const token = tokenEl.value.trim();
      const headers = {};
      if (token) {
        headers["Authorization"] = "Bearer " + token;
      }
      if (json) {
        headers["Content-Type"] = "application/json";
      }
      return headers;
    }

    async function requestJson(path, options = {}) {
      const response = await fetch(path, options);
      const text = await response.text();
      let payload = null;
      try {
        payload = text ? JSON.parse(text) : null;
      } catch {
        payload = { raw: text };
      }
      if (!response.ok) {
        throw new Error((payload && payload.error) ? payload.error : ("HTTP " + response.status));
      }
      return payload;
    }

    async function loadLastRun() {
      setStatus("Loading last run...", null);
      try {
        const data = await requestJson("/last-run", { method: "GET", headers: authHeaders(false) });
        lastRunEl.textContent = JSON.stringify(data, null, 2);
        setStatus("Loaded last run.", "ok");
      } catch (error) {
        setStatus("Failed to load last run: " + error.message, "error");
      }
    }

    async function runScan(recheck) {
      setStatus(recheck ? "Running full recheck..." : "Running scan...", null);
      try {
        const suffix = recheck ? "?recheck=1" : "";
        const data = await requestJson("/run" + suffix, { method: "POST", headers: authHeaders(false) });
        lastRunEl.textContent = JSON.stringify(data, null, 2);
        setStatus("Scan finished.", "ok");
        await loadLocations();
      } catch (error) {
        setStatus("Scan failed: " + error.message, "error");
      }
    }

    function abbreviateUrl(rawUrl) {
      try {
        const parsed = new URL(rawUrl);
        const compact = parsed.host + parsed.pathname;
        return compact.length > 72 ? compact.slice(0, 69) + "..." : compact;
      } catch {
        return rawUrl.length > 72 ? rawUrl.slice(0, 69) + "..." : rawUrl;
      }
    }

    async function loadLocations() {
      try {
        const data = await requestJson("/extra-locations", { method: "GET", headers: authHeaders(false) });
        const items = Array.isArray(data.extra_locations) ? data.extra_locations : [];
        if (!items.length) {
          locationsEl.innerHTML = "<p class='small'>No extra locations configured.</p>";
          return;
        }

        locationsEl.innerHTML = "";
        for (const entry of items) {
          const row = document.createElement("div");
          row.className = "item";

          const left = document.createElement("div");
          left.className = "item-main";
          const title = document.createElement("div");
          title.textContent = entry.toolhead;
          title.style.fontWeight = "600";
          const link = document.createElement("a");
          link.className = "small item-link";
          link.href = entry.url;
          link.target = "_blank";
          link.rel = "noopener noreferrer";
          link.title = entry.url;
          link.textContent = abbreviateUrl(entry.url);
          left.appendChild(title);
          left.appendChild(link);

          const del = document.createElement("button");
          del.className = "ghost";
          del.textContent = "Delete";
          del.addEventListener("click", async () => {
            setStatus("Deleting location...", null);
            try {
              await requestJson("/extra-locations", {
                method: "DELETE",
                headers: authHeaders(true),
                body: JSON.stringify({ toolhead: entry.toolhead, url: entry.url }),
              });
              setStatus("Location deleted.", "ok");
              await loadLocations();
            } catch (error) {
              setStatus("Delete failed: " + error.message, "error");
            }
          });

          row.appendChild(left);
          row.appendChild(del);
          locationsEl.appendChild(row);
        }
      } catch (error) {
        setStatus("Failed to load locations: " + error.message, "error");
      }
    }

    async function addLocation() {
      const toolhead = toolheadEl.value.trim();
      const url = urlEl.value.trim();
      if (!toolhead || !url) {
        setStatus("Toolhead and URL are required.", "error");
        return;
      }

      setStatus("Adding location...", null);
      try {
        await requestJson("/extra-locations", {
          method: "POST",
          headers: authHeaders(true),
          body: JSON.stringify({ toolhead, url }),
        });
        toolheadEl.value = "";
        urlEl.value = "";
        setStatus("Location added.", "ok");
        await loadLocations();
      } catch (error) {
        setStatus("Add failed: " + error.message, "error");
      }
    }

    document.getElementById("saveToken").addEventListener("click", () => {
      const saved = saveTokenValue(tokenEl.value.trim());
      setStatus(saved ? "Token saved in browser storage." : "Token set for this session only (browser storage unavailable).", saved ? "ok" : null);
    });

    document.getElementById("clearToken").addEventListener("click", () => {
      tokenEl.value = "";
      clearTokenValue();
      setStatus("Token cleared.", "ok");
    });

    document.getElementById("runScan").addEventListener("click", () => runScan(false));
    document.getElementById("runRecheck").addEventListener("click", () => runScan(true));
    document.getElementById("loadLast").addEventListener("click", loadLastRun);

    document.getElementById("addLocation").addEventListener("click", addLocation);
    document.getElementById("refreshLocations").addEventListener("click", loadLocations);

    // --- Blacklist ---
    const blacklistItemsEl = document.getElementById("blacklistItems");
    const blacklistToolheadEl = document.getElementById("blacklistToolhead");

    async function loadBlacklist() {
      try {
        const data = await requestJson("/blacklist", { method: "GET", headers: authHeaders(false) });
        const items = Array.isArray(data.blacklist) ? data.blacklist : [];
        if (!items.length) {
          blacklistItemsEl.innerHTML = "<p class='small'>No toolheads blacklisted.</p>";
          return;
        }

        blacklistItemsEl.innerHTML = "";
        for (const name of items) {
          const row = document.createElement("div");
          row.className = "item";

          const left = document.createElement("div");
          left.className = "item-main";
          left.textContent = name;
          left.style.fontWeight = "600";

          const del = document.createElement("button");
          del.className = "ghost";
          del.textContent = "Remove";
          del.addEventListener("click", async () => {
            setStatus("Removing from blacklist...", null);
            try {
              await requestJson("/blacklist", {
                method: "DELETE",
                headers: authHeaders(true),
                body: JSON.stringify({ toolhead: name }),
              });
              setStatus("Removed from blacklist.", "ok");
              await loadBlacklist();
            } catch (error) {
              setStatus("Remove failed: " + error.message, "error");
            }
          });

          row.appendChild(left);
          row.appendChild(del);
          blacklistItemsEl.appendChild(row);
        }
      } catch (error) {
        setStatus("Failed to load blacklist: " + error.message, "error");
      }
    }

    async function addToBlacklist() {
      const toolhead = blacklistToolheadEl.value.trim();
      if (!toolhead) {
        setStatus("Toolhead name is required.", "error");
        return;
      }

      setStatus("Adding to blacklist...", null);
      try {
        await requestJson("/blacklist", {
          method: "POST",
          headers: authHeaders(true),
          body: JSON.stringify({ toolhead }),
        });
        blacklistToolheadEl.value = "";
        setStatus("Added to blacklist.", "ok");
        await loadBlacklist();
      } catch (error) {
        setStatus("Add failed: " + error.message, "error");
      }
    }

    document.getElementById("addBlacklist").addEventListener("click", addToBlacklist);
    document.getElementById("refreshBlacklist").addEventListener("click", loadBlacklist);

    // --- Data Editor ---
    var editorData = null;
    var workingData = { toolheads: [], extruders: [], hotends: [], probes: [] };
    var currentCategory = "toolheads";
    var selectedIdx = -1;
    var pendingImages = {};

    var CATEGORY_FIELD_META = {
      toolheads: {
        name: { type: "edit", label: "Name" },
        title: { type: "edit", label: "Title" },
        url: { type: "edit", label: "URL" },
        build_guide: { type: "edit", label: "Build Guide" },
        description: { type: "edit", label: "Description" },
        category: { type: "change", label: "Category", optionsKey: "categoryOptions" },
        image: { type: "image", label: "Image" },
        configurator: { type: "toggle", label: "Configurator" },
        extruders: { type: "modify", label: "Extruders", optionsKey: "extruderNames" },
        hotend: { type: "modify", label: "Hotends", optionsKey: "hotendNames" },
        probe: { type: "modify", label: "Probes", optionsKey: "probeNames" },
        boards: { type: "modify", label: "Boards", optionsKey: "boards" },
        hotend_fan: { type: "modify", label: "Hotend Fan", optionsKey: "fans" },
        part_cooling_fan: { type: "modify", label: "Part Cooling Fan", optionsKey: "fans" },
        filament_cutter: { type: "change", label: "Filament Cutter", optionsKey: "filamentCutterOptions" },
        printer_compatibility: { type: "modify", label: "Printer Compatibility", optionsKey: "printerOptions" },
        belt_path: { type: "modify", label: "Belt Path", optionsKey: "beltPathOptions" }
      },
      extruders: {
        name: { type: "edit", label: "Name" },
        mounting_pattern: { type: "edit", label: "Mounting Pattern" },
        gear_type: { type: "edit", label: "Gear Type" },
        url: { type: "edit", label: "URL" },
        description: { type: "edit", label: "Description" },
        filament_sensor: { type: "edit", label: "Filament Sensor" },
        top_pick: { type: "toggle", label: "Top Pick" }
      },
      hotends: {
        name: { type: "edit", label: "Name" },
        mounting_pattern: { type: "modify", label: "Mounting Pattern", optionsKey: "_self" },
        length: { type: "edit", label: "Length" },
        meltzone_length: { type: "edit", label: "Meltzone Length" },
        hotend_type: { type: "edit", label: "Hotend Type" },
        flow_rate: { type: "edit", label: "Flow Rate" },
        nozzle_compatibility: { type: "modify", label: "Nozzle Compatibility", optionsKey: "_self" },
        url: { type: "edit", label: "URL" },
        description: { type: "edit", label: "Description" },
        top_pick: { type: "toggle", label: "Top Pick" }
      },
      probes: {
        name: { type: "edit", label: "Name" },
        type: { type: "change", label: "Type", optionsKey: "_self_enum" },
        url: { type: "edit", label: "URL" },
        description: { type: "edit", label: "Description" },
        top_pick: { type: "toggle", label: "Top Pick" }
      }
    };

    var CATEGORY_FIELD_ORDER = {
      toolheads: ["name", "title", "url", "build_guide", "description", "category", "image", "configurator",
        "extruders", "hotend", "probe", "boards", "hotend_fan", "part_cooling_fan", "filament_cutter",
        "printer_compatibility", "belt_path"],
      extruders: ["name", "mounting_pattern", "gear_type", "url", "description", "filament_sensor", "top_pick"],
      hotends: ["name", "mounting_pattern", "length", "meltzone_length", "hotend_type", "flow_rate", "nozzle_compatibility", "url", "description", "top_pick"],
      probes: ["name", "type", "url", "description", "top_pick"]
    };

    function getFieldMeta() { return CATEGORY_FIELD_META[currentCategory] || {}; }
    function getFieldOrder() { return CATEGORY_FIELD_ORDER[currentCategory] || []; }
    function getWorkingItems() { return workingData[currentCategory] || []; }

    function toArray(v) {
      if (v === null || v === undefined) return [];
      if (Array.isArray(v)) return v;
      return [String(v)];
    }

    function isPlaceholder(v) {
      if (!v) return true;
      var s = String(v).toLowerCase().trim();
      return s === "unknown" || s === "unknow" || s === "other" || s === "na" || s === "n/a" || s === "none" || s === "";
    }

    function cleanFieldForSave(arr) {
      var clean = arr.filter(function(v) { return !isPlaceholder(v); });
      if (clean.length === 0) return "unknown";
      if (clean.length === 1) return clean[0];
      return clean;
    }

    function esc(s) {
      return String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
    }

    function getField(item, key) {
      if (currentCategory === "toolheads") {
        if (key === "hotend_fan" && item.hotend_fans !== undefined) return item.hotend_fans;
        if (key === "part_cooling_fan" && item.part_cooling_fans !== undefined) return item.part_cooling_fans;
      }
      return item[key];
    }

    function setField(item, key, val) {
      if (currentCategory === "toolheads") {
        if (key === "hotend_fan" && "hotend_fans" in item) { item.hotend_fans = val; return; }
        if (key === "part_cooling_fan" && "part_cooling_fans" in item) { item.part_cooling_fans = val; return; }
      }
      item[key] = val;
    }

    function collectFieldValues(key) {
      var items = getWorkingItems();
      var vals = new Set();
      for (var i = 0; i < items.length; i++) {
        var v = getField(items[i], key);
        if (Array.isArray(v)) {
          for (var j = 0; j < v.length; j++) {
            if (v[j] && !isPlaceholder(v[j])) vals.add(String(v[j]));
          }
        } else if (v && !isPlaceholder(v)) {
          vals.add(String(v));
        }
      }
      return Array.from(vals).sort();
    }

    function getOptionsForKey(key) {
      var meta = getFieldMeta()[key];
      if (!meta) return [];
      if (meta.optionsKey === "_self") return collectFieldValues(key);
      if (meta.optionsKey === "_self_enum") return collectFieldValues(key);
      if (meta.optionsKey && editorData && editorData[meta.optionsKey]) return editorData[meta.optionsKey];
      return [];
    }

    async function editorLoad() {
      setStatus("Loading reference data...", null);
      try {
        editorData = await requestJson("/api/reference-data", { method: "GET", headers: authHeaders(false) });
        workingData = {
          toolheads: JSON.parse(JSON.stringify(editorData.toolheads)),
          extruders: JSON.parse(JSON.stringify(editorData.extruders)),
          hotends: JSON.parse(JSON.stringify(editorData.hotends)),
          probes: JSON.parse(JSON.stringify(editorData.probes))
        };
        editorPopulate();
        var cat = currentCategory;
        var count = (workingData[cat] || []).length;
        setStatus("Data loaded (" + count + " " + cat + ").", "ok");
      } catch (err) {
        setStatus("Failed to load reference data: " + err.message, "error");
      }
    }

    function editorPopulate() {
      var sel = document.getElementById("editorSelect");
      var items = getWorkingItems();
      sel.innerHTML = '<option value="">-- Select ' + currentCategory.replace(/s$/, "") + ' --</option>';
      items.sort(function(a, b) { return (a.name || "").localeCompare(b.name || ""); });
      for (var i = 0; i < items.length; i++) {
        var o = document.createElement("option");
        o.value = String(i);
        o.textContent = items[i].name;
        sel.appendChild(o);
      }
      selectedIdx = -1;
      editorRender();
    }

    function editorRender() {
      var c = document.getElementById("editorDetail");
      var items = getWorkingItems();
      if (selectedIdx < 0 || selectedIdx >= items.length) {
        c.innerHTML = '<p class="small">Select an item or click "+ New" to create one.</p>';
        document.getElementById("editorPRBtn").disabled = true;
        document.getElementById("editorScanBtn").disabled = true;
        return;
      }
      document.getElementById("editorPRBtn").disabled = false;
      document.getElementById("editorScanBtn").disabled = !(currentCategory === "toolheads" && items[selectedIdx] && items[selectedIdx].url);
      var item = items[selectedIdx];
      var fieldMeta = getFieldMeta();
      var fieldOrder = getFieldOrder();
      var h = '<div class="field-grid">';

      for (var i = 0; i < fieldOrder.length; i++) {
        var key = fieldOrder[i];
        var meta = fieldMeta[key];
        if (!meta) continue;
        var val = getField(item, key);
        h += '<div class="field-row"><div class="field-label">' + esc(meta.label) + '</div><div class="field-value">';
        if (meta.type === "modify") {
          var arr = toArray(val);
          if (!arr.length) { h += '<span class="text-val empty">none</span>'; }
          else {
            h += '<div class="chips">';
            for (var j = 0; j < arr.length; j++) {
              h += '<span class="chip' + (isPlaceholder(arr[j]) ? ' unknown' : '') + '">' + esc(arr[j]) + '</span>';
            }
            h += '</div>';
          }
        } else if (meta.type === "toggle") {
          h += '<span class="text-val">' + (val ? "true" : "false") + '</span>';
        } else if (meta.type === "change") {
          h += '<span class="text-val">' + esc(String(val || "unknown")) + '</span>';
        } else {
          h += val ? '<span class="text-val">' + esc(String(val)) + '</span>' : '<span class="text-val empty">empty</span>';
        }
        h += '</div><div class="field-actions">';
        if (meta.type === "modify") h += '<button class="ghost btn-sm" data-modify="' + key + '">Modify</button>';
        else if (meta.type === "toggle") h += '<button class="ghost btn-sm" data-toggle="' + key + '">Toggle</button>';
        else if (meta.type === "change") h += '<button class="ghost btn-sm" data-enum="' + key + '">Change</button>';
        else if (meta.type === "image") h += '<button class="ghost btn-sm" data-edit="' + key + '">Edit</button> <button class="ghost btn-sm" data-upload="image">Upload</button>';
        else h += '<button class="ghost btn-sm" data-edit="' + key + '">Edit</button>';
        h += '</div></div>';
      }

      var allKeys = Object.keys(item);
      for (var k = 0; k < allKeys.length; k++) {
        var aKey = allKeys[k];
        if (fieldOrder.indexOf(aKey) >= 0) continue;
        if (currentCategory === "toolheads" && (aKey === "hotend_fans" || aKey === "part_cooling_fans")) continue;
        var aVal = item[aKey];
        var displayVal = aVal;
        if (Array.isArray(aVal)) displayVal = aVal.join(", ");
        h += '<div class="field-row"><div class="field-label">' + esc(aKey) + '</div><div class="field-value">';
        h += '<span class="text-val">' + esc(String(displayVal == null ? "" : displayVal)) + '</span>';
        h += '</div><div class="field-actions"><button class="ghost btn-sm" data-edit="' + esc(aKey) + '">Edit</button></div></div>';
      }
      h += '</div>';
      c.innerHTML = h;

      c.querySelectorAll("[data-edit]").forEach(function(b) {
        b.addEventListener("click", function() { editorTextEdit(b.getAttribute("data-edit")); });
      });
      c.querySelectorAll("[data-upload]").forEach(function(b) {
        b.addEventListener("click", editorImageUpload);
      });
      c.querySelectorAll("[data-toggle]").forEach(function(b) {
        b.addEventListener("click", function() {
          var k = b.getAttribute("data-toggle");
          var items = getWorkingItems();
          items[selectedIdx][k] = !items[selectedIdx][k];
          editorRender();
        });
      });
      c.querySelectorAll("[data-enum]").forEach(function(b) {
        b.addEventListener("click", function() { editorEnumEdit(b.getAttribute("data-enum")); });
      });
      c.querySelectorAll("[data-modify]").forEach(function(b) {
        b.addEventListener("click", function() { editorListModify(b.getAttribute("data-modify")); });
      });
    }

    function openModal(html) {
      document.getElementById("modalContent").innerHTML = html;
      document.getElementById("modalOverlay").classList.add("open");
    }

    function closeModal() {
      document.getElementById("modalOverlay").classList.remove("open");
    }

    function editorTextEdit(key) {
      var items = getWorkingItems();
      var item = items[selectedIdx];
      var cur = getField(item, key) || "";
      var big = key === "description" || key === "url";
      var inp = big
        ? '<textarea id="modalInput" style="width:100%;min-height:80px;">' + esc(cur) + '</textarea>'
        : '<input id="modalInput" type="text" value="' + esc(cur) + '" />';
      var meta = getFieldMeta()[key];
      var label = meta ? meta.label : key;
      openModal(
        '<h3>Edit ' + esc(label) + '</h3>' + inp +
        '<div class="row" style="margin-top:12px;"><button id="modalSave">Save</button> <button class="ghost" id="modalCancel">Cancel</button></div>'
      );
      document.getElementById("modalSave").addEventListener("click", function() {
        setField(item, key, document.getElementById("modalInput").value);
        closeModal(); editorRender();
      });
      document.getElementById("modalCancel").addEventListener("click", closeModal);
    }

    function editorEnumEdit(key) {
      var items = getWorkingItems();
      var item = items[selectedIdx];
      var cur = getField(item, key) || "unknown";
      var meta = getFieldMeta()[key];
      var label = meta ? meta.label : key;

      function renderEnumModal() {
        var opts = getOptionsForKey(key).slice();
        if (cur && opts.indexOf(cur) < 0) opts = [cur].concat(opts);
        var oh = "";
        for (var i = 0; i < opts.length; i++) {
          oh += '<option value="' + esc(opts[i]) + '"' + (opts[i] === cur ? " selected" : "") + '>' + esc(opts[i]) + '</option>';
        }
        openModal(
          '<h3>Change ' + esc(label) + '</h3>' +
          '<select id="modalSelect" style="width:100%;">' + oh + '</select>' +
          '<div style="margin-top:8px;margin-bottom:12px;">' +
          '<input id="modalCustomVal" type="text" placeholder="Or type a custom value" style="width:calc(100% - 70px);display:inline-block;" />' +
          ' <button id="modalCustomUse" class="alt btn-sm">Use</button></div>' +
          '<div class="row"><button id="modalSave">Save</button> <button class="ghost" id="modalCancel">Cancel</button></div>'
        );
        document.getElementById("modalCustomUse").addEventListener("click", function() {
          var v = document.getElementById("modalCustomVal").value.trim();
          if (!v) return;
          var optKey = meta ? meta.optionsKey : null;
          if (optKey && optKey !== "_self" && optKey !== "_self_enum" && editorData && Array.isArray(editorData[optKey]) && editorData[optKey].indexOf(v) < 0) {
            editorData[optKey].push(v);
            editorData[optKey].sort();
          }
          cur = v;
          renderEnumModal();
        });
        document.getElementById("modalSave").addEventListener("click", function() {
          setField(item, key, document.getElementById("modalSelect").value);
          closeModal(); editorRender();
        });
        document.getElementById("modalCancel").addEventListener("click", closeModal);
      }
      renderEnumModal();
    }

    function editorListModify(key) {
      var items = getWorkingItems();
      var item = items[selectedIdx];
      var arr = toArray(getField(item, key)).slice();
      var allOpts = getOptionsForKey(key);

      function renderListModal() {
        var ih = "";
        for (var i = 0; i < arr.length; i++) {
          ih += '<div class="modal-item"><span>' + esc(arr[i]) + '</span>' +
            '<button class="ghost btn-sm btn-danger" data-del="' + i + '">Remove</button></div>';
        }
        if (!arr.length) ih = '<p class="small">No items.</p>';

        var avail = allOpts.filter(function(o) {
          return arr.map(function(a) { return a.toLowerCase(); }).indexOf(o.toLowerCase()) < 0;
        });
        var oh = '<option value="">-- Select --</option>';
        for (var j = 0; j < avail.length; j++) {
          oh += '<option value="' + esc(avail[j]) + '">' + esc(avail[j]) + '</option>';
        }
        var meta = getFieldMeta()[key];
        var label = meta ? meta.label : key;

        openModal(
          '<h3>Modify ' + esc(label) + '</h3>' +
          '<div class="modal-items">' + ih + '</div>' +
          '<div class="modal-add"><select id="modalAddSel">' + oh + '</select>' +
          '<button id="modalAddBtn" class="alt btn-sm">Add</button></div>' +
          '<div style="margin-top:6px;margin-bottom:12px;">' +
          '<input id="modalCustomVal" type="text" placeholder="Or type a custom value" style="width:calc(100% - 70px);display:inline-block;" />' +
          ' <button id="modalCustomAdd" class="alt btn-sm">Add</button></div>' +
          '<div class="row"><button id="modalDone">Done</button></div>'
        );

        document.querySelectorAll("[data-del]").forEach(function(b) {
          b.addEventListener("click", function() {
            arr.splice(parseInt(b.getAttribute("data-del")), 1);
            renderListModal();
          });
        });
        document.getElementById("modalAddBtn").addEventListener("click", function() {
          var v = document.getElementById("modalAddSel").value;
          if (!v) return;
          arr = arr.filter(function(x) { return !isPlaceholder(x); });
          arr.push(v);
          renderListModal();
        });
        document.getElementById("modalCustomAdd").addEventListener("click", function() {
          var v = document.getElementById("modalCustomVal").value.trim();
          if (!v) return;
          arr = arr.filter(function(x) { return !isPlaceholder(x); });
          arr.push(v);
          renderListModal();
        });
        document.getElementById("modalDone").addEventListener("click", function() {
          setField(item, key, cleanFieldForSave(arr));
          closeModal(); editorRender();
        });
      }
      renderListModal();
    }

    function editorImageUpload() {
      var fi = document.createElement("input");
      fi.type = "file";
      fi.accept = "image/*";
      fi.addEventListener("change", function() {
        var f = fi.files[0];
        if (!f) return;
        var items = getWorkingItems();
        var item = items[selectedIdx];
        var itemName = (item.name || "item").replace(/[^a-zA-Z0-9_-]/g, "_");
        var destFilename = itemName + ".webp";
        setStatus("Optimizing image...", null);

        var img = new Image();
        img.onload = function() {
          var MAX_WIDTH = 1200;
          var w = img.width;
          var h = img.height;
          if (w > MAX_WIDTH) {
            h = Math.round(h * MAX_WIDTH / w);
            w = MAX_WIDTH;
          }
          var canvas = document.createElement("canvas");
          canvas.width = w;
          canvas.height = h;
          var ctx = canvas.getContext("2d");
          ctx.drawImage(img, 0, 0, w, h);
          canvas.toBlob(function(blob) {
            var reader = new FileReader();
            reader.onload = function() {
              var imgKey = currentCategory + "_" + selectedIdx;
              pendingImages[imgKey] = { filename: destFilename, content: reader.result.split(",")[1] };
              item.image = "/" + destFilename;
              editorRender();
              setStatus("Image staged: " + destFilename + " (optimized to WebP)", "ok");
            };
            reader.readAsDataURL(blob);
          }, "image/webp", 0.80);
        };
        img.onerror = function() {
          setStatus("Failed to load image for optimization.", "error");
        };
        var objectUrl = URL.createObjectURL(f);
        img.src = objectUrl;
      });
      fi.click();
    }

    function editorNewItem() {
      if (!editorData) { setStatus("Load data first.", "error"); return; }
      var catLabel = currentCategory.replace(/s$/, "");
      var name = prompt("Enter new " + catLabel + " name:");
      if (!name || !name.trim()) return;
      name = name.trim();
      var items = getWorkingItems();
      for (var i = 0; i < items.length; i++) {
        if ((items[i].name || "").toLowerCase() === name.toLowerCase()) {
          setStatus(catLabel + " already exists: " + name, "error");
          return;
        }
      }
      var newItem = { name: name };
      var fieldOrder = getFieldOrder();
      var fieldMeta = getFieldMeta();
      for (var j = 0; j < fieldOrder.length; j++) {
        var k = fieldOrder[j];
        if (k === "name") continue;
        var m = fieldMeta[k];
        if (!m) continue;
        if (m.type === "toggle") newItem[k] = false;
        else if (m.type === "modify") newItem[k] = "unknown";
        else if (m.type === "change") newItem[k] = "unknown";
        else if (m.type === "image") newItem[k] = "";
        else newItem[k] = "";
      }
      items.push(newItem);
      editorPopulate();
      var sel = document.getElementById("editorSelect");
      for (var s = 0; s < sel.options.length; s++) {
        if (sel.options[s].textContent === name) {
          sel.value = sel.options[s].value;
          selectedIdx = parseInt(sel.options[s].value);
          break;
        }
      }
      editorRender();
      setStatus("New " + catLabel + " added: " + name, "ok");
    }

    function editorManageFields() {
      if (!editorData) { setStatus("Load data first.", "error"); return; }
      var fieldMeta = getFieldMeta();
      var fieldOrder = getFieldOrder();
      var items = getWorkingItems();

      function renderFieldsModal() {
        var ih = '<h3>Manage Fields (' + currentCategory + ')</h3>';
        ih += '<div class="modal-items">';
        for (var i = 0; i < fieldOrder.length; i++) {
          var key = fieldOrder[i];
          var meta = fieldMeta[key];
          if (!meta) continue;
          ih += '<div class="modal-item"><span>' + esc(meta.label) + ' <span class="small">(' + esc(meta.type) + ')</span></span>';
          if (key !== "name") {
            ih += '<button class="ghost btn-sm" data-chgtype="' + esc(key) + '">Type</button>';
            ih += ' <button class="ghost btn-sm btn-danger" data-rmfield="' + esc(key) + '">Remove</button>';
          }
          ih += '</div>';
        }
        ih += '</div>';
        ih += '<div style="margin-top:12px;border-top:1px dashed var(--line);padding-top:12px;">';
        ih += '<h4 style="margin:0 0 8px;">Add New Field</h4>';
        ih += '<div style="margin-bottom:6px;"><label>Field Name</label><input id="newFieldName" type="text" placeholder="e.g. weight" /></div>';
        ih += '<div style="margin-bottom:6px;"><label>Field Type</label>';
        ih += '<select id="newFieldType" style="width:100%;">';
        ih += '<option value="edit">Edit (text entry)</option>';
        ih += '<option value="toggle">Toggle (boolean)</option>';
        ih += '<option value="modify">Modify (pick from list)</option>';
        ih += '<option value="change">Change (select from options)</option>';
        ih += '<option value="image">Image Upload</option>';
        ih += '</select></div>';
        ih += '<button id="addFieldBtn" class="alt" style="width:100%;">Add Field</button>';
        ih += '</div>';
        ih += '<div class="row" style="margin-top:12px;"><button id="modalDone">Done</button></div>';
        openModal(ih);

        document.querySelectorAll("[data-chgtype]").forEach(function(b) {
          b.addEventListener("click", function() {
            var fk = b.getAttribute("data-chgtype");
            var curMeta = fieldMeta[fk];
            var typeOpts = ["edit", "toggle", "modify", "change", "image"];
            var so = typeOpts.map(function(t) {
              return '<option value="' + t + '"' + (t === curMeta.type ? " selected" : "") + '>' + t + '</option>';
            }).join("");
            openModal(
              '<h3>Change Type: ' + esc(curMeta.label) + '</h3>' +
              '<label>New Type</label>' +
              '<select id="chgTypeSelect" style="width:100%;margin-bottom:12px;">' + so + '</select>' +
              '<div class="row"><button id="chgTypeSave">Save</button> <button class="ghost" id="chgTypeCancel">Cancel</button></div>'
            );
            document.getElementById("chgTypeSave").addEventListener("click", function() {
              var newType = document.getElementById("chgTypeSelect").value;
              if (newType !== curMeta.type) {
                curMeta.type = newType;
                if (newType === "modify") {
                  if (!curMeta.optionsKey || curMeta.optionsKey === "_self_enum") curMeta.optionsKey = "_self";
                } else if (newType === "change") {
                  if (!curMeta.optionsKey || curMeta.optionsKey === "_self") curMeta.optionsKey = "_self_enum";
                } else {
                  delete curMeta.optionsKey;
                }
                var defaultVal = "";
                if (newType === "toggle") defaultVal = false;
                else if (newType === "modify" || newType === "change") defaultVal = "unknown";
                for (var x = 0; x < items.length; x++) {
                  if (newType === "toggle" && typeof items[x][fk] !== "boolean") items[x][fk] = defaultVal;
                  else if ((newType === "modify" || newType === "change") && items[x][fk] == null) items[x][fk] = defaultVal;
                  else if ((newType === "edit" || newType === "image") && typeof items[x][fk] !== "string") items[x][fk] = String(items[x][fk] == null ? "" : items[x][fk]);
                }
                setStatus("Field '" + fk + "' type changed to " + newType + ".", "ok");
              }
              renderFieldsModal();
            });
            document.getElementById("chgTypeCancel").addEventListener("click", renderFieldsModal);
          });
        });

        document.querySelectorAll("[data-rmfield]").forEach(function(b) {
          b.addEventListener("click", function() {
            var fk = b.getAttribute("data-rmfield");
            if (!confirm("Remove field '" + fk + "' from all " + items.length + " " + currentCategory + "?")) return;
            delete fieldMeta[fk];
            var idx = fieldOrder.indexOf(fk);
            if (idx >= 0) fieldOrder.splice(idx, 1);
            for (var x = 0; x < items.length; x++) {
              delete items[x][fk];
            }
            renderFieldsModal();
            setStatus("Field '" + fk + "' removed from all " + currentCategory + ".", "ok");
          });
        });

        document.getElementById("addFieldBtn").addEventListener("click", function() {
          var fname = document.getElementById("newFieldName").value.trim();
          var ftype = document.getElementById("newFieldType").value;
          if (!fname) { setStatus("Field name is required.", "error"); return; }
          // Field keys are snake_case to match JSON conventions
          var fieldKey = fname.toLowerCase().replace(/[^a-z0-9_]/g, "_");
          if (fieldMeta[fieldKey]) { setStatus("Field '" + fieldKey + "' already exists.", "error"); return; }

          var newMeta = { type: ftype, label: fname };
          // _self: collect unique values from all items for list picking (modify)
          // _self_enum: collect unique values from all items for dropdown selection (change)
          if (ftype === "modify") newMeta.optionsKey = "_self";
          if (ftype === "change") newMeta.optionsKey = "_self_enum";
          fieldMeta[fieldKey] = newMeta;
          fieldOrder.push(fieldKey);

          var defaultVal = "";
          if (ftype === "toggle") defaultVal = false;
          else if (ftype === "modify") defaultVal = "unknown";
          else if (ftype === "change") defaultVal = "unknown";
          for (var x = 0; x < items.length; x++) {
            if (items[x][fieldKey] === undefined) items[x][fieldKey] = defaultVal;
          }

          renderFieldsModal();
          setStatus("Field '" + fname + "' (" + ftype + ") added to all " + items.length + " " + currentCategory + ".", "ok");
        });

        document.getElementById("modalDone").addEventListener("click", function() {
          closeModal(); editorRender();
        });
      }
      renderFieldsModal();
    }

    async function editorScanToolhead() {
      if (currentCategory !== "toolheads" || selectedIdx < 0) return;
      var items = getWorkingItems();
      var item = items[selectedIdx];
      if (!item || !item.url) {
        setStatus("No URL set for this toolhead.", "error");
        return;
      }
      setStatus("Scanning " + item.name + "...", null);
      document.getElementById("editorScanBtn").disabled = true;
      try {
        var result = await requestJson("/api/scan-toolhead", {
          method: "POST",
          headers: authHeaders(true),
          body: JSON.stringify({ url: item.url })
        });
        var fieldMap = [
          ["extruders", "extruders"],
          ["hotend", "hotends"],
          ["probe", "probes"],
          ["boards", "boards"],
          ["hotend_fan", "hotend_fans"],
          ["part_cooling_fan", "part_cooling_fans"],
          ["printer_compatibility", "printer_compatibility"],
          ["belt_path", "belt_path"]
        ];
        for (var i = 0; i < fieldMap.length; i++) {
          var itemKey = fieldMap[i][0];
          var resultKey = fieldMap[i][1];
          var arr = result[resultKey];
          if (arr && arr.length > 0) {
            setField(item, itemKey, arr.length === 1 ? arr[0] : arr);
          }
        }
        if (result.filament_cutter) {
          setField(item, "filament_cutter", result.filament_cutter);
        }
        editorRender();
        var found = [];
        if (result.extruders.length) found.push(result.extruders.length + " extruder(s)");
        if (result.hotends.length) found.push(result.hotends.length + " hotend(s)");
        if (result.probes.length) found.push(result.probes.length + " probe(s)");
        if (result.boards.length) found.push(result.boards.length + " board(s)");
        if (result.hotend_fans.length) found.push(result.hotend_fans.length + " hotend fan(s)");
        if (result.part_cooling_fans.length) found.push(result.part_cooling_fans.length + " part cooling fan(s)");
        if (result.filament_cutter) found.push("filament cutter");
        if (result.printer_compatibility.length) found.push(result.printer_compatibility.length + " printer(s)");
        if (result.belt_path.length) found.push(result.belt_path.length + " belt path(s)");
        setStatus("Scan complete: " + (found.length ? found.join(", ") : "no components found") + ".", found.length ? "ok" : "error");
      } catch (err) {
        setStatus("Scan failed: " + err.message, "error");
      } finally {
        document.getElementById("editorScanBtn").disabled = false;
      }
    }

    async function editorCreatePR() {
      if (!workingData.toolheads.length && !workingData.extruders.length && !workingData.hotends.length && !workingData.probes.length) return;
      setStatus("Creating pull request...", null);
      try {
        var images = [];
        for (var key in pendingImages) {
          if (pendingImages.hasOwnProperty(key)) {
            images.push(pendingImages[key]);
          }
        }
        var body = {
          toolheads: workingData.toolheads,
          extruders: workingData.extruders,
          hotends: workingData.hotends,
          probes: workingData.probes
        };
        if (images.length) body.images = images;
        var res = await requestJson("/api/create-pr", {
          method: "POST",
          headers: authHeaders(true),
          body: JSON.stringify(body)
        });
        setStatus("Pull request created!", "ok");
        openModal(
          '<h3>Pull Request Created</h3>' +
          '<p><a href="' + esc(res.pr_url) + '" target="_blank" rel="noopener" style="color:#8cb3ff;">' + esc(res.pr_url) + '</a></p>' +
          '<div class="row" style="margin-top:12px;"><button id="modalDone">Close</button></div>'
        );
        document.getElementById("modalDone").addEventListener("click", closeModal);
        pendingImages = {};
      } catch (err) {
        setStatus("PR failed: " + err.message, "error");
      }
    }

    // Tab switching
    document.querySelectorAll(".tab-btn").forEach(function(b) {
      b.addEventListener("click", function() {
        document.querySelectorAll(".tab-btn").forEach(function(x) { x.classList.remove("active"); });
        document.querySelectorAll(".tab-content").forEach(function(x) { x.classList.remove("active"); });
        b.classList.add("active");
        document.getElementById("tab-" + b.getAttribute("data-tab")).classList.add("active");
      });
    });

    // Editor event bindings
    document.getElementById("editorCategory").addEventListener("change", function() {
      currentCategory = this.value;
      selectedIdx = -1;
      if (editorData) editorPopulate();
    });
    document.getElementById("editorSelect").addEventListener("change", function() {
      selectedIdx = this.value ? parseInt(this.value) : -1;
      editorRender();
    });
    document.getElementById("editorNewBtn").addEventListener("click", editorNewItem);
    document.getElementById("editorFieldsBtn").addEventListener("click", editorManageFields);
    document.getElementById("editorLoadBtn").addEventListener("click", editorLoad);
    document.getElementById("editorScanBtn").addEventListener("click", editorScanToolhead);
    document.getElementById("editorPRBtn").addEventListener("click", editorCreatePR);
    document.getElementById("modalOverlay").addEventListener("click", function(e) {
      if (e.target === this) closeModal();
    });

    loadLocations();
    loadBlacklist();
    loadLastRun();
  <\/script>
</body>
</html>`;
}
__name(dashboardHtml, "dashboardHtml");
function isAuthorized(request, env) {
  const expected = env.MANUAL_RUN_TOKEN?.trim();
  if (!expected) {
    return false;
  }
  const header = request.headers.get("authorization") ?? "";
  return header === `Bearer ${expected}`;
}
__name(isAuthorized, "isAuthorized");
function requireAuthorization(request, env) {
  if (!env.MANUAL_RUN_TOKEN?.trim()) {
    return jsonResponse({ error: "MANUAL_RUN_TOKEN is not configured." }, 503);
  }
  if (!isAuthorized(request, env)) {
    return jsonResponse({ error: "Unauthorized." }, 401);
  }
  return null;
}
__name(requireAuthorization, "requireAuthorization");
async function saveLastRun(env, report) {
  await env.SCANNER_STATE.put(LAST_RUN_KEY, JSON.stringify(report, null, 2));
}
__name(saveLastRun, "saveLastRun");
async function getLastRun(env) {
  return await env.SCANNER_STATE.get(LAST_RUN_KEY, "json");
}
__name(getLastRun, "getLastRun");
async function maybeSendNotification(env, report) {
  if (!report.ok || !report.changed || report.results.length === 0) {
    return { delivered: false, reason: "No change notification needed." };
  }
  const apiKey = env.RESEND_API_KEY?.trim();
  const from = env.NOTIFY_EMAIL_FROM?.trim();
  const to = env.NOTIFY_EMAIL_TO?.split(",").map((entry) => entry.trim()).filter(Boolean) ?? [];
  if (!apiKey || !from || to.length === 0) {
    return { delivered: false, reason: "Notification settings are incomplete." };
  }
  const bodies = buildEmailBodies(report.results);
  const response = await fetch("https://api.resend.com/emails", {
    method: "POST",
    headers: {
      authorization: `Bearer ${apiKey}`,
      "content-type": "application/json"
    },
    body: JSON.stringify({
      from,
      to,
      subject: `Toolhead Scanner: ${report.changeCount} toolhead(s) changed`,
      text: bodies.text,
      html: bodies.html
    })
  });
  if (!response.ok) {
    const message = await response.text();
    throw new Error(`Notification send failed: ${response.status} ${message}`);
  }
  return { delivered: true };
}
__name(maybeSendNotification, "maybeSendNotification");
async function runAndPersist(env, trigger, recheck = false) {
  const report = await runScan(env, {
    trigger,
    recheck,
    log: /* @__PURE__ */ __name((message) => console.log(`[${trigger}] ${message}`), "log")
  });
  try {
    const notification = await maybeSendNotification(env, report);
    report.logs.push(
      notification.delivered ? "Notification delivered" : `Notification skipped: ${notification.reason ?? "unknown reason"}`
    );
  } catch (error) {
    report.logs.push(`Notification error: ${error instanceof Error ? error.message : String(error)}`);
  }
  if (report.ok && report.changed && report.updatedPayload.toolheads.length > 0 && env.GITHUB_TOKEN?.trim()) {
    try {
      const changedNames = report.results.map((r) => r.name).join(", ");
      const prResult = await createDataPR(env, {
        toolheads: report.updatedPayload.toolheads,
        message: `ToolheadScanner: update ${report.changeCount} toolhead(s) \u2014 ${changedNames}`
      });
      report.logs.push(`PR created: ${prResult.pr_url}`);
    } catch (error) {
      report.logs.push(`PR creation error: ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  await saveLastRun(env, report);
  return report;
}
__name(runAndPersist, "runAndPersist");
async function handleExtraLocations(request, env) {
  if (request.method === "GET") {
    return jsonResponse({ extra_locations: await loadExtraLocations(env) });
  }
  const payload = await request.json();
  if (request.method === "POST") {
    const added = await addExtraLocation(env, String(payload.toolhead ?? ""), String(payload.url ?? ""));
    return jsonResponse({ added, extra_locations: await loadExtraLocations(env) }, added ? 201 : 200);
  }
  if (request.method === "DELETE") {
    const removed = await removeExtraLocation(env, String(payload.toolhead ?? ""), String(payload.url ?? ""));
    return jsonResponse({ removed, extra_locations: await loadExtraLocations(env) });
  }
  if (request.method === "PUT") {
    await saveExtraLocations(env, payload.extra_locations ?? []);
    return jsonResponse({ extra_locations: await loadExtraLocations(env) });
  }
  return jsonResponse({ error: "Method not allowed." }, 405);
}
__name(handleExtraLocations, "handleExtraLocations");
async function handleBlacklist(request, env) {
  if (request.method === "GET") {
    return jsonResponse({ blacklist: await loadBlacklist(env) });
  }
  const payload = await request.json();
  if (request.method === "POST") {
    const added = await addBlacklistEntry(env, String(payload.toolhead ?? ""));
    return jsonResponse({ added, blacklist: await loadBlacklist(env) }, added ? 201 : 200);
  }
  if (request.method === "DELETE") {
    const removed = await removeBlacklistEntry(env, String(payload.toolhead ?? ""));
    return jsonResponse({ removed, blacklist: await loadBlacklist(env) });
  }
  if (request.method === "PUT") {
    await saveBlacklist(env, payload.blacklist ?? []);
    return jsonResponse({ blacklist: await loadBlacklist(env) });
  }
  return jsonResponse({ error: "Method not allowed." }, 405);
}
__name(handleBlacklist, "handleBlacklist");
async function handleReferenceData(env) {
  const data = await loadEditorData(env.TOOLHEAD_DATA_SOURCE_BASE);
  return jsonResponse(data);
}
__name(handleReferenceData, "handleReferenceData");
async function createDataPR(env, payload) {
  const githubToken = env.GITHUB_TOKEN?.trim();
  if (!githubToken) {
    throw new Error("GITHUB_TOKEN is not configured.");
  }
  const hasToolheads = Array.isArray(payload.toolheads) && payload.toolheads.length > 0;
  const hasExtruders = Array.isArray(payload.extruders) && payload.extruders.length > 0;
  const hasHotends = Array.isArray(payload.hotends) && payload.hotends.length > 0;
  const hasProbes = Array.isArray(payload.probes) && payload.probes.length > 0;
  if (!hasToolheads && !hasExtruders && !hasHotends && !hasProbes) {
    throw new Error("At least one data array (toolheads, extruders, hotends, or probes) is required.");
  }
  const UPSTREAM_OWNER = "SartorialGrunt0";
  const UPSTREAM_REPO = "ToolheadBuilder";
  async function gh(path, reqOptions = {}) {
    const resp = await fetch(`https://api.github.com${path}`, {
      ...reqOptions,
      headers: {
        Authorization: `Bearer ${githubToken}`,
        Accept: "application/vnd.github+json",
        "User-Agent": "ToolheadScannerWorker/1.0",
        "X-GitHub-Api-Version": "2022-11-28",
        "Content-Type": "application/json",
        ...reqOptions.headers ?? {}
      }
    });
    const text = await resp.text();
    let data;
    try {
      data = JSON.parse(text);
    } catch {
      data = { raw: text };
    }
    if (!resp.ok) {
      throw new Error(`GitHub API ${resp.status}: ${typeof data.message === "string" ? data.message : JSON.stringify(data)}`);
    }
    return data;
  }
  __name(gh, "gh");
  try {
    await gh(`/repos/${UPSTREAM_OWNER}/${UPSTREAM_REPO}/forks`, { method: "POST", body: "{}" });
  } catch {
  }
  const user = await gh("/user");
  const forkOwner = user.login;
  try {
    await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/merge-upstream`, {
      method: "POST",
      body: JSON.stringify({ branch: "main" })
    });
  } catch {
  }
  const mainRef = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/ref/heads/main`);
  const baseSha = mainRef.object.sha;
  const branchName = `data-update-${Date.now()}`;
  await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/refs`, {
    method: "POST",
    body: JSON.stringify({ ref: `refs/heads/${branchName}`, sha: baseSha })
  });
  const treeEntries = [];
  const changedFiles = [];
  const dataFiles = [
    { key: "toolheads", wrapKey: "toolheads", path: "src/data/toolheads.json", data: payload.toolheads },
    { key: "extruders", wrapKey: "extruders", path: "src/data/extruders.json", data: payload.extruders },
    { key: "hotends", wrapKey: "hotends", path: "src/data/hotends.json", data: payload.hotends },
    { key: "probes", wrapKey: "probes", path: "src/data/probes.json", data: payload.probes }
  ];
  for (const df of dataFiles) {
    if (Array.isArray(df.data) && df.data.length > 0) {
      const fileContent = JSON.stringify({ [df.wrapKey]: df.data }, null, 2) + "\n";
      const fileBlob = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/blobs`, {
        method: "POST",
        body: JSON.stringify({ content: fileContent, encoding: "utf-8" })
      });
      treeEntries.push({ path: df.path, mode: "100644", type: "blob", sha: fileBlob.sha });
      changedFiles.push(df.key);
    }
  }
  if (payload.images && payload.images.length > 0) {
    for (const img of payload.images) {
      if (img.content && img.filename) {
        const sanitized = img.filename.replace(/[^a-zA-Z0-9._-]/g, "_");
        const imageBlob = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/blobs`, {
          method: "POST",
          body: JSON.stringify({ content: img.content, encoding: "base64" })
        });
        treeEntries.push({ path: `public/${sanitized}`, mode: "100644", type: "blob", sha: imageBlob.sha });
      }
    }
  }
  const baseCommit = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/commits/${baseSha}`);
  const baseTreeSha = baseCommit.tree.sha;
  const newTree = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/trees`, {
    method: "POST",
    body: JSON.stringify({ base_tree: baseTreeSha, tree: treeEntries })
  });
  const commitMessage = payload.message || `Update ${changedFiles.join(", ")} via ToolheadScanner`;
  const newCommit = await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/commits`, {
    method: "POST",
    body: JSON.stringify({ message: commitMessage, tree: newTree.sha, parents: [baseSha] })
  });
  await gh(`/repos/${forkOwner}/${UPSTREAM_REPO}/git/refs/heads/${branchName}`, {
    method: "PATCH",
    body: JSON.stringify({ sha: newCommit.sha })
  });
  const pr = await gh(`/repos/${UPSTREAM_OWNER}/${UPSTREAM_REPO}/pulls`, {
    method: "POST",
    body: JSON.stringify({
      title: commitMessage,
      head: `${forkOwner}:${branchName}`,
      base: "main",
      body: `Automated update from ToolheadScanner Dashboard.

Updated files: ${changedFiles.join(", ")}`
    })
  });
  return { pr_url: pr.html_url, pr_number: pr.number, branch: branchName };
}
__name(createDataPR, "createDataPR");
async function handleCreatePR(request, env) {
  const payload = await request.json();
  try {
    const result = await createDataPR(env, payload);
    return jsonResponse(result);
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    const status = message.includes("not configured") ? 503 : 400;
    return jsonResponse({ error: message }, status);
  }
}
__name(handleCreatePR, "handleCreatePR");
var index_default = {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (request.method === "GET" && url.pathname === "/health") {
      return jsonResponse({ ok: true, service: "toolheadscanner", time: (/* @__PURE__ */ new Date()).toISOString() });
    }
    if (request.method === "GET" && url.pathname === "/") {
      return textResponse([
        "ToolheadScanner Cloudflare Worker",
        "",
        "GET  /dashboard             Web dashboard for scanner operations",
        "POST /run?recheck=1         Trigger a manual scan",
        "GET  /last-run              Fetch the last stored scan report",
        "GET  /extra-locations       List KV-backed extra GitHub locations",
        "POST /extra-locations       Add one extra location",
        "DELETE /extra-locations     Remove one extra location",
        "GET  /blacklist             List blacklisted toolheads",
        "POST /blacklist             Add a toolhead to the blacklist",
        "DELETE /blacklist           Remove a toolhead from the blacklist",
        "GET  /api/reference-data    Fetch reference data for editor (toolheads, extruders, hotends, probes)",
        "POST /api/scan-toolhead     Scan a single toolhead URL and return found components",
        "POST /api/create-pr         Create a GitHub PR with data changes"
      ].join("\n"));
    }
    if (request.method === "GET" && url.pathname === "/dashboard") {
      return htmlResponse(dashboardHtml());
    }
    if (url.pathname === "/run" && request.method === "POST") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      const recheck = url.searchParams.get("recheck") === "1" || url.searchParams.get("recheck") === "true";
      const report = await runAndPersist(env, "manual", recheck);
      return jsonResponse(report, report.ok ? 200 : 500);
    }
    if (url.pathname === "/last-run" && request.method === "GET") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      const report = await getLastRun(env);
      return jsonResponse(report ?? { message: "No scan has run yet." });
    }
    if (url.pathname === "/extra-locations") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      return await handleExtraLocations(request, env);
    }
    if (url.pathname === "/blacklist") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      return await handleBlacklist(request, env);
    }
    if (url.pathname === "/api/reference-data" && request.method === "GET") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      return await handleReferenceData(env);
    }
    if (url.pathname === "/api/scan-toolhead" && request.method === "POST") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      const body = await request.json();
      const targetUrl = String(body.url ?? "").trim();
      if (!targetUrl) {
        return jsonResponse({ error: "url is required." }, 400);
      }
      const result = await scanSingleToolhead(targetUrl);
      return jsonResponse(result);
    }
    if (url.pathname === "/api/create-pr" && request.method === "POST") {
      const authError = requireAuthorization(request, env);
      if (authError) {
        return authError;
      }
      return await handleCreatePR(request, env);
    }
    return jsonResponse({ error: "Not found." }, 404);
  },
  async scheduled(_event, env, ctx) {
    ctx.waitUntil(runAndPersist(env, "cron", false));
  }
};
export {
  index_default as default
};
//# sourceMappingURL=index.js.map
